# CONSOLIDATED EDIT LIST â€” URmessage spec review (5 reviewers merged)

29 distinct defects after dedup: **12 BLOCKER**, **26 MAJOR**, **5 MINOR**. Duplicate reports merged; where two reviewers proposed different fixes for the same defect, the merged entry states the reconciliation and flags it as a decision.

---

## 1. BLOCKERS â€” fix before any implementation plan

**B1. Â§5.2 â€” delete `device_root` from the master_key tree.** *(M13; 1 reviewer, reinforced by B8)*
`device_root(d) = HKDF-Expand(master_key, "device/v1" â€– LP(d), 32)` makes every per-device secret a function of the master seed, so any device that can derive its own keys holds `master_key` and therefore `recovery_root` â€” DEVICE_REVOKE becomes cryptographically meaningless.
**Edit:** Remove `device_root` and all four sub-roots (`sign_root`, `kem_root`, `cap_root`, `notif_root`) from the master-derived tree. Device keys MUST be generated on-device from a local CSPRNG or a device-local seed sealed to the platform keystore/DPAPI, and enter group state only via DEVICE_ADD carrying public halves, signed by the `self_signing` key of an already-authorized device. State normatively that `recovery_root` is the only master-derived group-usable secret. Cascades into Â§5.3 inventory, Â§5.4 loss table, Â§5.5 provisioning.

**B2. Â§9.2 â€” the record key schedule is both nonce-reusing and not a ratchet.** *(M10 + A6/G3/F8; 2 reviewers, same section, one joint rewrite)*
(a) One `key_i â€– nonce_i` pair is derived per record but Â§7 encrypts **two** ciphertexts (`ct_head`, `ct_body`) under it with identical AAD â€” straight XChaCha20-Poly1305 nonce reuse; keystream XOR recovers both plaintexts and the Poly1305 key is recoverable. (b) `key_i` is a direct indexed derivation from a retained `sender_key`, so anyone holding the class root recomputes every key forever â€” Â§9.2's "generations ratcheted past are deleted" and Â§9.3's "(iii) deleted from the device once ratcheted past" describe an operation the construction cannot perform.
**Edit:** Rewrite Â§9.2 as one block: `chain_key[0] = sender_key`; `chain_key[i+1] = HKDF-Expand(chain_key[i], "ratchet/v1", 32)`; `key_head â€– nonce_head = HKDF-Expand(chain_key[i], "rec/v1/head", 56)`; `key_body â€– nonce_body = HKDF-Expand(chain_key[i], "rec/v1/body", 56)`. Require sender and receiver to overwrite `chain_key[i]` after use with a bounded skipped-key window. Make ephemeral roots **time-sliced** (`K_eph_root[n][b][t]`), not index-sliced. State normatively that `ct_head` is ALWAYS encrypted under the DURABLE class root, since it is always retained and carries chain/attribution data. Fix Â§9.3 and Â§12.4 to match.

**B3. Â§7 â€” redefine `cap_auth` to cover the full cleartext header.** *(M8 + F1/F17; 2 reviewers, converging fixes)*
`cap_auth` "computed LAST over the finished ct_head â€– ct_body; outside the AAD" authenticates none of the header the server acts on. Every Â§10.1/Â§10.3 server duty â€” gapless `stream_index` per `(writer_handle, epoch_hint)`, CAS on `claimed_new_epoch` gated by `epoch_commit_flag`, pruning by `retention_class`/`expire_at` â€” operates on unauthenticated fields. AAD binding is verifiable only by a key holder, which the server is not; the proxying home server (Â§10.5) can alter any of them undetectably.
**Edit:** `cap_auth = Auth(cap, "URmessage/v1/cap" â€– LP(server_nonce) â€– LP(group_id) â€– LP(writer_handle) â€– u32(epoch_hint) â€– u64(stream_index) â€– LP(prev_record_hash) â€– u8(retention_class) â€– u8(size_bucket) â€– u64(expire_at) â€– u8(epoch_commit_flag) â€– u32(claimed_new_epoch) â€– LP(H(ct_head)) â€– LP(body_hash))` â€” everything except server-assigned `record_id`. **Decision needed:** one reviewer omitted `server_nonce`; include it (anti-replay across hosts) unless the challenge-response of Â§3.3 already supplies freshness. `body_hash` is the new field from B7 â€” these two edits must land together.

**B4. Â§6.3 / Â§7 / Â§10.7 â€” `control_seq` and `control_head_hash` are used at five sites and defined at none.** *(M3/M4 + A3/F2; 2 reviewers)*
Both appear in the ct_head plaintext, the Â§7 Ed25519 preimage, Â§6.3's hard-fail and fork-alarm rules, and Â§10.7 â€” with no definition of what the head hashes, who increments the counter, or how it is assigned. The Â§7 signature is literally not implementable. A per-group total order over all control events would need a linearization point the spec removed (chains are per-writer, Â§6.2; Â§10.7 permits host-free injection), so two admins proposing concurrently would trip each other's fork alarm.
**Edit:** Define the control chain over **epoch-advancing commits only**: `control_head_hash = transcript_hash[epoch]` (equivalently `H(canonical(EPOCH_COMMIT[n]))`, identical to the next commit's `prev_epoch_head_hash`), and `control_seq` = the per-group monotone count of accepted commits, assigned by the Â§10.3 CAS. State that non-epoch-advancing proposals are unordered, carry the head of the last commit they saw, have no sequence number of their own, and are bound only by the committing EPOCH_COMMIT's `proposals[]`. Rewrite Â§6.3's hard-fail and fork-alarm rules against that definition so concurrent proposals cannot raise a fork alarm.

**B5. Â§9.1 / Â§8.1 â€” the epoch chain has no base case and GENESIS carries no key material.** *(M5)*
`epoch_secret[n] = HKDF-Extract(salt = DeriveSecret(epoch_secret[n-1], "init"), ikm = commit_secret[n])` never defines `epoch_secret[0]`, and the Â§8.1 GENESIS payload lists no wraps of any kind â€” only pubkeys, policy and a founder bootstrap `cap_pub`. No founding member other than the creator can obtain key material; a group cannot be created as specified.
**Edit:** `epoch_secret[0] = HKDF-Extract(salt = "URmessage/v1/genesis" â€– group_id, ikm = commit_secret[0])`, `commit_secret[0]` fresh CSPRNG at creation. Add to the GENESIS payload row: `commit_secret_wraps[{device_id, hybrid_ct}]`, `eph_root_wraps[...]`, `recovery_wraps[{member_id, hybrid_ct}]` for every founding member, plus `transcript_hash[0]` and `member_hash[0]`. Define `context[0]` (see M9 below).

**B6. Â§5.4 / Â§9.1 / Â§5.5 â€” seed-only restore has no chain anchor.** *(M13/M14)*
Â§5.4 says the recovery wrap carries `commit_secret[n]` only; Â§9.1 puts the `epoch_secret[n]` joiner anchor in `joiner_epoch_secret_wraps[]` targeted at devices, and recovery keys are member-scoped and excluded. A member who joined at epoch 40 holds `commit_secret[40..]` but no `epoch_secret[39]` to extract against â€” the chain never anchors and seed-only restore recovers nothing, breaking the Â§15 recovery promise.
**Edit:** State normatively that `joiner_epoch_secret_wraps[]` targets **both** the joining member's active device KEM keys **and** their recovery key `rk_*`, carrying `epoch_secret[join_epoch]` in each; the recovery restore path uses that wrap as its anchor, then applies `commit_secret[n]` from `recovery_wraps` forward. Update the Â§5.4 table row to "commit_secret[n], plus epoch_secret[join_epoch] at join".

**B7. Â§6.2 / Â§7 / Â§6.4 â€” `redacted_form(r)` consumes the field erasure destroys.** *(A2/A4/F4)*
`redacted_form(r)` includes `H(ct_body)`, but `ct_body` is the erasable field and Â§6.4 lets servers prune control payloads for epochs < E. Once erased, `prev_record_hash` of the next record is unverifiable and the chain breaks exactly at the erasure point â€” defeating F4/A4 ("erasing a payload leaves a verifiable hole rather than breaking the chain") and making GDPR erasure and Â§6.4 compaction chain-destroying.
**Edit:** Add a retained cleartext header field `body_hash 32B` to the Â§7 RECORD header (and to the AAD and to `cap_auth`, per B3), defined as `H(ct_body)` at write time. Redefine `redacted_form(r)` to consume `body_hash`. Add to Â§6.4: "A server or client erasing `ct_body` MUST retain `body_hash`; a record whose `body_hash` is absent MUST be treated as chain-broken."

**B8. Â§5.2 / Â§10.2 â€” `writer_handle` is derived from a device-private secret but must be recomputable by every member.** *(F17/M5/F1)*
`writer_handle` and `cap` both derive from `cap_root`, a device-private secret. Â§10.2 asserts "clients recompute every `writer_handle` from the roster they already validated" â€” impossible. Worse, the committer publishes `EPOCH_WRITER_SET{entries[{writer_handle, cap_pub, may_write}]}` and cannot derive any other device's handle either.
**Edit:** Split the two. (a) `writer_handle(d,n) = HKDF-Expand(epoch_secret[n], "wh/v1" â€– LP(group_id) â€– LP(device_id), 16)` â€” every member holds `epoch_secret[n]`, the server does not; delete the `cap_root`-based derivation from Â§5.2. (b) Keep the capability **private** key device-only, but make its public key publicly derivable via key blinding so the committer can populate `cap_pub`. Consistent with B1 (`cap_root` becomes device-local, not master-derived).

**B9. Â§10.7 vs Â§10.1/Â§6.2 â€” host-independent control events and gapless `stream_index` are mutually exclusive.** *(B4/F10)*
Â§10.7: a relayed control event "is written into the emitting device's stream at its next `stream_index` regardless of host acceptance." Â§10.1/Â§6.2: the server enforces gapless `stream_index` per `(writer_handle, epoch_hint)`. If index k never reaches the host, index k+1 must be rejected â€” the writer is bricked at that host from the first refused relay.
**Edit:** State the resolution in both Â§10.7 and Â§10.1: (a) `stream_index` is assigned write-once and locally â€” a device MUST durably record "index k consumed" **before** encrypting and MUST NEVER encrypt a second record at a consumed index, even if the first was refused; (b) servers enforce **monotonicity-with-declared-holes**, not strict contiguity: a writer may submit k+1 with a declared hole at k, and the hole renders as a detected gap.

**B10. Â§10.3 vs Â§8.1 â€” `SERVER_MIGRATE` deadlocks against a refusing host.** *(B4/F10)*
Â§10.3 quarantines relayed epoch-advancing commits "until a host acknowledges them or the group migrates via `SERVER_MIGRATE`", but Â§8.1 states membership events change nothing until referenced by an EPOCH_COMMIT and lists SERVER_MIGRATE with an effective epoch. Migrating away from a refusing host thus requires a commit the refusing host will not acknowledge. A host that simply refuses commits holds the group hostage permanently.
**Edit:** Add an explicit carve-out to Â§10.3 and to the Â§8.1 `SERVER_MIGRATE` / `HOST_SUCCESSION_SET` rows: these two events take effect on their signatures alone (two admins per Â§14) plus the `member_device_quorum` named in `activation_rule`, WITHOUT host acknowledgement and WITHOUT being carried by an EPOCH_COMMIT. State they are the only events exempt from the Â§10.3 quarantine, and define what the first commit at the new host must reconcile.

**B11. Â§3.3 / Â§10.2 / Â§10.5 â€” the foreign host demands a capability the client never presents to it.** *(B2/F11/F15/M4)*
Â§3.3 requires group subscription and writes to complete challenge-response against a per-group capability key; Â§10.5 requires the client to speak only to its home server, which fetches from foreign hosts. Mutually exclusive: either the home server holds the capability key (defeating the point) or the client speaks to the foreign host (contradicting Â§10.5).
**Edit:** Add a read capability to Â§5.2 â€” `read_cap = HKDF-Expand(cap_root, "read/v1" â€– LP(g) â€– u32(epoch), 32)` â€” with its public part in `EPOCH_WRITER_SET` entries alongside `may_write` as `may_read`. Add to Â§10.5 a signed, short-lived, read-only delegation `READ_DELEGATION{group_id, epoch, home_server_id, expiry, sig_by_read_cap}` the client issues to its home server. Keep the **write** capability undelegated: `cap_auth` is computed to and verified by the group host (see B3 and MAJ-13).

**B12. Â§10.2 / Â§10.3 â€” an evicted member can forge the epoch-n writer set.** *(1 reviewer; blocker-class because it grants server-accepted write capability post-eviction)*
`EPOCH_WRITER_SET` for epoch n is authenticated "under a capability key valid in the previous epoch" â€” which every member removed at epoch n still holds. The evicted member forges a set listing themselves as a writer and races the legitimate committer; no CAS is specified for the writer set (Â§10.3's CAS covers only commit records) and the server cannot distinguish the two.
**Edit:** The server MUST accept an `EPOCH_WRITER_SET` for epoch n only when submitted atomically, in the same request as, the commit record that wins the Â§10.3 CAS for `claimed_new_epoch = n`, and MUST reject any later set for that epoch. Additionally include `H(canonical(EPOCH_WRITER_SET))` inside the signed EPOCH_COMMIT payload so clients verify it too.

> **Blocker interlock:** B3 and B7 share the new `body_hash` field; B1, B8 and B11 all restructure `cap_root`; B2 and B7 both change what `ct_head` must always carry; B4 and MAJ-16 both touch `control_head_hash`. Apply as one coherent pass over Â§5â€“Â§10, not as isolated patches.

---

## 2. MAJOR â€” ranked by rework prevented

Ranked highest first by how much downstream work a late fix would destroy: wire-format-freezing items (Â§16 freezes the format in slice 1) precede security-property items, which precede prose/disclosure items.

**MAJ-1. Â§7 / Â§8.1 / Â§8.3 â€” `canonical(GENESIS)` has two incompatible definitions.** *(M2)* Â§7 and Â§8.3 say `group_id = H(canonical(GENESIS))` over the whole event; Â§8.1 says `initial_host` is signed but not in the preimage. Â§8.3's joiner check is unimplementable without an exact excluded-field list.
**Edit:** Introduce `GENESIS_CORE` and define `group_id = H(canonical(GENESIS_CORE))`, fields in order: version, group_nonce, founding members (sorted by member_id, full pubkey set + join_nonce), admin set, cipher suite, retention defaults. State that `initial_host`, the founder bootstrap `cap_pub`, and all genesis wraps are in GENESIS but outside GENESIS_CORE.

**MAJ-2. Â§7 â€” pad plaintexts to `size_bucket`.** *(F18/A8)* `size_bucket` enumerates 256B/1K/4K/16K/64K/blob-ref and Â§10.6 leans on bucketing, but no normative padding rule exists anywhere; without it ciphertext length reveals true size and the field is decorative.
**Edit:** After the Â§7 record layout: "Before encryption, `ct_body` plaintext MUST be padded with zero bytes to exactly the declared `size_bucket` byte count, with the true length carried as a `u32` prefix inside the padded plaintext. A record whose decrypted `ct_body` length â‰  its declared bucket size MUST be rejected. `ct_head` is padded to a fixed 256-byte plaintext."

**MAJ-3. Â§7 â€” five header fields are in the AAD but not in `sig`.** *(M4)* `retention_class`, `size_bucket`, `expire_at`, `epoch_commit_flag`, `claimed_new_epoch`. Since every member can derive every other member's `sender_key` (Â§9.2), any member can re-encrypt an untouched INNER â€” original signature verbatim â€” under a different AAD: an EPH message republished as DURABLE.
**Edit:** Append `â€– LP(H(AAD))` to the Â§7 signature preimage (or list the five fields explicitly). Keep `record_id` excluded.

**MAJ-4. Â§5.2 / Â§8.1 â€” device KEM keys have no rotation input.** *(M12)* `gek_cls = X25519(HKDF-Expand(kem_root, "x25519/v1" â€– LP(g), 32))` is a pure function of (device, group_id): leaving a hostile group and rejoining reproduces byte-identical keys, and MEMBER_UPDATE cannot actually rotate anything.
**Edit:** `gek_cls = X25519(HKDF-Expand(kem_root, "x25519/v1" â€– LP(g) â€– LP(join_nonce) â€– u32(key_gen), 32))`, same for `gek_pq`, with `key_gen` published in DEVICE_ADD and incremented by MEMBER_UPDATE. Add `new_member_id` and `new_key_gen` to MEMBER_UPDATE, signed by the outgoing key.

**MAJ-5. Â§8.1 â€” `proposals[]` has no defined encoding.** *(M3/M8)* It cannot be `record_id`s (server-assigned, never covered by a client MAC). As opaque references, `transcript_hash[n]` does not bind proposal content, so a host serving different proposal bodies to different clients produces identical transcript hashes and confirmation tags â€” reopening the divergent-view hole M3 closed.
**Edit:** Define `proposals[]` as an ordered list of `H(canonical(proposal INNER))`. A client MUST NOT accept an EPOCH_COMMIT until it holds and has validated every referenced proposal body, and MUST reject a commit whose proposal set does not produce the committed `member_hash[n]` when applied to the previous roster.

**MAJ-6. Â§9.1 â€” eviction by wrap omission is undetectable.** *(M5/M6)* A malicious committer can publish a commit whose `member_hash[n]` correctly encodes the full roster â€” so everyone else's confirmation tag verifies â€” while omitting one victim's entry from `commit_secret_wraps[]` / `recovery_wraps[]`. The victim is evicted with no MEMBER_REMOVE ever signed and no other client alarmed.
**Edit:** Add to Â§9.1: "A client MUST reject an EPOCH_COMMIT unless `commit_secret_wraps[]` covers exactly the set of active device_ids in `member_hash[n]`, `eph_root_wraps[]` covers exactly the same set, and `recovery_wraps[]` covers exactly the member_id set in `member_hash[n]` minus members with the recovery opt-out. Any missing, extra, or duplicate wrap target is a hard failure."

**MAJ-7. Â§10.3 â€” any authorized writer can burn epoch numbers.** *(B4/F10)* The server cannot see `type`, so it linearizes on client-supplied `epoch_commit_flag`/`claimed_new_epoch`. Any writer submits garbage with `epoch_commit_flag=1, claimed_new_epoch=current+1`; `cap_auth` verifies, the head advances, and the real EPOCH_COMMIT is permanently rejected.
**Edit:** Add `may_commit` to `EPOCH_WRITER_SET` entries (Â§10.2), set only for admins and for members on own-device-only commits per Â§14. In Â§10.3: "A server MUST reject `epoch_commit_flag = 1` from a `writer_handle` whose entry does not carry `may_commit`." Define the server's group head as `{current_epoch, last_accepted_commit_record_id}` and specify an admin recovery path for a burned epoch number.

**MAJ-8. Â§10.2 â€” `cap` is used for two incompatible primitives, neither named.** *(vs Â§13.3 alg_id registry)* `cap_auth` must be a deniable NaCl-box authenticator (needs an X25519 keypair + peer server key), while `EPOCH_WRITER_SET.sig` is "authenticated under a capability key valid in the previous epoch" â€” if a signature, that needs Ed25519 and is exactly the transferable proof Â§10.2 forbids one paragraph earlier.
**Edit:** `cap` seeds an X25519 keypair (`alg_id 0x0011`); `cap_auth` is `crypto_box` authentication between that key and the accepting server's registry-pinned key (Â§10.4), and is deniable. Rename `EPOCH_WRITER_SET.sig` to `set_auth` and define it as the same box authenticator to the accepting server â€” the set is client-verifiable via handle recomputation, so no transferable signature is needed.

**MAJ-9. Â§9.1 â€” `DeriveSecret`'s context parameter is ambiguous.** *(M3)* `DeriveSecret(s, label)` hard-codes `context[n]`, but `salt = DeriveSecret(epoch_secret[n-1], "init")` takes the previous epoch's secret. Both readings compute; two implementations silently derive different epoch secrets and fail only at confirmation-tag time.
**Edit:** State explicitly that `DeriveSecret(epoch_secret[n-1], "init")` uses `context[n-1]` and every other invocation uses the context of the epoch whose secret is its input. Write it longhand: `init_secret[n-1] = HKDF-Expand(epoch_secret[n-1], "URmessage/v1/init" â€– context[n-1], 32)`. Define `context[0]` for the genesis anchor (pairs with B5).

**MAJ-10. Â§8.1 â€” MEMBERSHIP_CHECKPOINT has no position binding.** *(A5, B3/B5)* The payload drops `epoch`, `control_seq`, and `control_head_hash`, so nothing binds a checkpoint to a position in the log and the Â§8.1 WELCOME rule cannot verify that the countersigned `control_head` and the enclosed checkpoint describe the same moment â€” an inviter or replaying host can pair a stale checkpoint with a fresh head.
**Edit:** Restore all three fields to the payload. Add to the WELCOME acceptance rule: "A joiner MUST reject a WELCOME whose enclosed checkpoint's `control_head_hash` is not equal to, or reachable backwards from, the countersigned `control_head`."

**MAJ-11. Â§6.4 / Â§8.1 â€” `authorized_writer_digest` cannot preserve attribution.** *(A5/F5)* A bare hash only verifies a set you already possess; after the epoch's DEVICE_ADD/MEMBER_ADD payloads are pruned nobody possesses it, so Â§6.4's claim that compacted history "stays attributable rather than becoming decryptable-but-anonymous" is false as specified.
**Edit:** Change the checkpoint field to enumerable data: `authorized_writers[epoch] â†’ sorted list of {device_id, member_id, device_sign_pub, writer_handle}` for every epoch since the last checkpoint, plus a Merkle root over the list for incremental proofs. Update Â§6.4's claim and Â§12.2's enumeration rule to reference the list.

**MAJ-12. Â§8.1 / Â§14 â€” MEMBERSHIP_CHECKPOINT is authorized by a single admin.** *(A5)* The checkpoint is the ground truth a first-time joiner validates against and the artifact a group resumes from after host death; a forgery retroactively rewrites the member set, the device set, and (per MAJ-11) the whole attribution record for compacted epochs â€” squarely the "one compromised admin causes irreversible harm" class Â§14 already carves out.
**Edit:** Move MEMBERSHIP_CHECKPOINT into Â§14's two-admin-signature list and change the Â§8.1 authorizer to "two admins" (owner alone suffices in a single-admin group, per the existing Â§14 sentence). Add to Â§6.4: "Payloads for epochs < E MUST NOT be discarded until a checkpoint at E carrying the required signature count has validated."

**MAJ-13. Â§10.5 â€” the write path is never specified.** *(vs Â§10.1, Â§10.3)* Â§10.5 is stated purely in terms of reads while every Â§10.1 server duty is assigned to an unqualified "the server." If writes are proxied, the home server sits between the writer and the CAS and can delay, reorder or drop them.
**Edit:** State normatively that writes are proxied identically to reads; that `cap_auth` is computed to and verified by the **group host**, not the home server, so the home server cannot mint or alter accepted records; that the home server is a blind relay for writes and MUST forward the CAS result verbatim; and that a client MUST treat a missing or delayed host acknowledgement as an unresolved write and reconcile before consuming the next `stream_index`. (Pairs with B3 and B11.)

**MAJ-14. Â§10.4 â€” FETCH_ATTESTATION is in the wrong namespace.** *(B6/F20)* It covers `record_ids_returned[]`, but `record_id` is server-assigned after acceptance and never client-nameable, so a client can never test an omitted record against the attestation. The omission-detection namespace everywhere else in the document is `(writer_handle, stream_index)`.
**Edit:** Redefine as `{group_id, requested_range, epoch_hint, per_writer[{writer_handle, index_ranges_returned, high_water_index}], server_time, server_id, sig}`. Keep `record_ids_returned[]` for pagination if useful, but make the signed evidentiary content the index ranges, putting Â§12.2's STREAM_DIGEST comparison and the attestation in one namespace.

**MAJ-15. Â§10.5 â€” the attestation is lost at the hop with the most leverage.** *(B6/B2, F20/F11)* The foreign host issues its attestation to the home server, not the client; nothing requires relay, and nothing requires the home server to attest to what it served from cache.
**Edit:** Add to Â§10.5: "A home server MUST relay the foreign host's FETCH_ATTESTATION verbatim with every proxied response, and MUST additionally emit its own FETCH_ATTESTATION signed by its registry-bound key covering the range it served from cache. A client MUST retain both and MUST surface a durable warning when either omits a record it later learns of."

**MAJ-16. Â§8.1 / Â§8.3 â€” WELCOME countersignatures have no freshness binding and are not out of band.** *(B5/F6)* They sit inside the server-side encrypted container (F6 required out-of-band delivery, dropped), and they cover a `control_head` fixed at container-creation time â€” so anyone who can serve the container replays a stale-but-valid invite.
**Edit:** Require the joiner to emit `JOIN_REQUEST` carrying a fresh 32-byte `joiner_nonce`; both countersignatures MUST cover `control_head â€– checkpoint_hash â€– joiner_nonce` and MUST be produced after the JOIN_REQUEST. Add to Â§8.1's acceptance rule that a joiner MUST reject any countersignature not covering its own nonce.

**MAJ-17. Â§10.2 â€” `EPOCH_WRITER_SET` entry order leaks cross-epoch linkage.** Entries are cleartext with no ordering requirement; emitted in roster order (the natural implementation), the server links a device's epoch-n handle to its epoch-(n+1) handle by list position, and `may_write` marks the same members every epoch â€” reconstructing exactly the linkage per-epoch handles exist to prevent.
**Edit:** Require `entries[]` sorted ascending by `writer_handle`; servers MUST reject out-of-order or duplicate-handle sets. Note the residual disclosed leak stays per-epoch device and observer counts, as Â§10.6/Â§15 already say.

**MAJ-18. Â§5.2 / Â§8.1 â€” `notif_root` has no per-group expansion.** *(M6/F19)* Â§5.3 scopes the notification key "Device Ã— group"; Â§5.2 stops at `notif_root` and DEVICE_ADD carries one `notif_pub`, so the same credential is presented to every group's host â€” joinable across groups to a single device.
**Edit:** Add under `notif_root`: `notif(g) = HKDF-Expand(notif_root, "notif/v1" â€– LP(g), 32)`; change Â§8.1's `notif_pub` to the per-group public key (DEVICE_ADD is already per-group). State in Â§10.6 that a host sees a credential unlinkable to any other group's.

**MAJ-19. Â§14 / Â§8.1 â€” owner loss is permanent in a single-admin group.** *(M9)* Â§14 gives succession to "a two-admin quorum," which can never exist in the default single-admin group (or a DM). Membership, admin set, retention policy, host migration and succession freeze forever while chat keeps working â€” M9 required admin loss to stop failing open.
**Edit:** Give the GENESIS-committed designated successor a self-elevation path: allow ADMIN_SET_UPDATE authorized by the successor's device plus the `member_device_quorum` already defined for HOST_SUCCESSION_SET's `activation_rule`, after the `unreachable_for` period. Require a persistent client banner naming the successor and the quorum.

**MAJ-20. Â§8 / Â§14 â€” DM is confirmed sound as a design but never instantiated.** *(round-1 D2)* Nothing says how a DM is created, who is in its admin set, whether either party may MEMBER_ADD a third participant (turning a DM into a group without consent), what ROLE_SET means with two members, what blocking does, or whether the pairwise fallback is in scope.
**Edit:** Add Â§8.4 "DM instantiation": a DM is a group whose GENESIS names both members as OWNER-equivalent admins with `dm_locked = true` committed in GENESIS_CORE; MEMBER_ADD MUST be rejected while `dm_locked` is set (converting to a group requires creating a new group); ROLE_SET and HISTORY_GRANT are unavailable; either party may unilaterally SERVER_MIGRATE and DEVICE_REVOKE their own devices.

**MAJ-21. Â§5.5 vs Â§15 â€” history scope contradiction, and DEVICE_ADD has no scope field.** *(A7/F7)* Â§5.5's provisioning bundle carries `K_durable_root` for epochs within the member's `history_scope`, so a new device receives history back to the day the **member** joined; Â§15 promises the opposite.
**Edit:** Keep Â§5.5's behaviour (it is the stated multi-device promise) and reword Â§15 to "History follows you to a new device without anyone mailing a private key around, and without any device reaching back past the day *you* joined the group." Add a `history_scope` field to DEVICE_ADD in Â§8.1 so the granted range is committed on the wire.

**MAJ-22. Â§10.7 vs Â§10.5/Â§10.8 â€” peer-to-peer control relay rebuilds the social graph in Postgres.** *(B2/B7, F11/F12)* Device-to-device relays create `transfer_contract` rows storing `source_network_id, destination_network_id` indexed both directions and retained up to 90 days â€” the member-to-member graph Â§10.8 exists to prevent.
**Edit:** Restrict the default path: "A member device relays control events via its own home server, which forwards to the target member's home server. Direct device-to-device relay is a fallback used only when both home servers are unreachable, MUST use a provider-terminated hop per Â§10.8, and MUST be surfaced to the user as a degraded mode." Add a sentence to Â§10.8 acknowledging the contract rows peer relay creates.

**MAJ-23. Â§12.2 vs Â§6.2 â€” offline devices cannot satisfy the epoch-boundary digest rule.** Â§6.2 requires a writer's final record in an epoch to be a STREAM_DIGEST and Â§12.2 requires warnings when a writer publishes none at a boundary; an offline device learns the epoch ended only after its stream for that epoch is closed, so every offline device generates a permanent false alarm.
**Edit:** Relax to: a device MUST emit a STREAM_DIGEST covering an epoch on its first write after learning the epoch closed, carried in the **new** epoch's stream via the `handle_link` structure Â§6.2 already defines (`{prev_epoch, prev_writer_handle, prev_high_water_index, prev_head_hash}`). Replace the boundary warning with two states: "digest pending" (silent) until a grace period elapses, then alarm.

**MAJ-24. Â§7 / Â§10.6 / Â§15 â€” `epoch_commit_flag` and `claimed_new_epoch` are an undisclosed control-plane event feed.** These cleartext fields hand the host "an epoch changed at time T, advancing to N, written by W" permanently, and COVER records cannot blur them (a cover with the flag set would corrupt the CAS).
**Edit:** Keep the fields (Â§10.3 needs them) but disclose and bound them: add "epoch commit timing and the epoch counter" to Â§10.6's Foreign host list, add a sentence to Â§15's "Worse than Signal" paragraph, and add the mitigation "Clients SHOULD emit epoch commits on a jittered schedule and MAY emit no-op commits, so commit timing does not imply a membership change." Note the no-op allowance in Â§10.3.

**MAJ-25. Â§5.2 closing paragraph â€” P2 restated in the form round 1 marked OVERSOLD.** "The server therefore cannot link a writer across epochs, across groups, or to a member" contradicts Â§10.6 and Â§15, which say the home server sees the account, the full group list, and all read/write timing.
**Edit:** Rewrite scoped to the value, not the vantage point: "A `writer_handle` value carries no cross-epoch, cross-group, or member linkage by construction. This is a property of the identifier only: the FOREIGN host of a group sees nothing more; the user's HOME server sees the account, the full group list, and all read/write timing regardless (Â§10.6, Â§15)."

**MAJ-26. Â§5.2 / Â§5.3 / Â§11.2 â€” cross-signing (`user_signing`) finding is TRUNCATED in the source material.** *(M2/F14)* The reviewer's text ends mid-sentence ("`user_signing` â€¦ is derived in Â§5.2 and"). The defect concerns `user_signing` â€” Ed25519, signs others' masters after out-of-band verification â€” against Â§11.2 cross-signing.
**Edit:** Re-fetch this finding from the originating reviewer before the fix pass; do not guess the edit. Flagged here so it is not silently dropped.

---

## 3. MINOR

1. **Â§13.2** â€” ML-KEM guardrails missing: require `crypto/mlkem`, make `NewEncapsulationKey1024`/`NewDecapsulationKey1024` errors and seed length â‰  64 hard validation failures, forbid accepting a hybrid wrap with either component missing, and forbid `alg_id` 0x0013 silently degrading to 0x0011. *(M7)*
2. **Â§7** â€” state that `epoch_hint` MUST equal the INNER `epoch`, that a mismatch is a reject, and that the INNER `epoch` governs all key and authorization resolution.
3. **Â§15** â€” "no conflicting-history problem" is oversold (P3); reword to "exactly one linearization point for membership and epoch changesâ€¦ content records are ordered per writer, not globally."
4. **Â§8.3** â€” specify the invite link structure: `https://<host>/i/<base64url(container_id 16B â€– container_key 16B)>`, container sealed under XChaCha20-Poly1305 keyed by `HKDF-Expand(container_key, "URmessage/v1/invite", 32)`, server stores ciphertext only, one-time invites delete the container on first successful JOIN_REQUEST.
5. **Â§14 / Â§8.1** â€” commit admin thresholds as data in GENESIS_CORE (`policy{membership_k, history_grant_k, migrate_k, succession_k, unreachable_for}`, defaults 1/2/2/2/72h) updatable via ADMIN_SET_UPDATE, so Â§17 item 5 can be resolved without a wire-format break. *(M9)*

---

## 4. VERDICT

**No.** Fixing the 12 BLOCKERs alone leaves the document internally inconsistent for review, because the blocker fixes themselves introduce or relocate wire-format fields â€” `body_hash`, the GENESIS wraps, the `epoch_secret`-derived `writer_handle`, the widened `cap_auth` preimage â€” and at least eight MAJORs (MAJ-1 through MAJ-8) also change fields that Â§16 freezes in slice 1, so a reviewer reading the blocker-only version would be reviewing a format that is already known to be changing. Hand it over after BLOCKERs plus MAJ-1 through MAJ-8 and MAJ-10 land in a single pass over Â§5â€“Â§10; the remaining MAJORs and all MINORs are prose, disclosure, or policy edits that can be resolved during the user's review rather than before it.
