[
    {
        "severity":  "BLOCKER",
        "location":  "p2-crypto-primitives.md Â§\"What this plan needs from other plans\" (L292â€“293) and Tasks 12â€“15 (L2825, 2876, 2962â€“2963, 3003, 3026â€“3032, 3096â€“3097, 3133, 3251â€“3252, 3312, 3424â€“3425) vs p1-syntax-codec.md Tasks 3, 5, 8, 13",
        "problem":  "p2 consumes `syntax.WriteVarVec(dst []byte, v []byte) []byte`, `syntax.WriteUint16(dst []byte, v uint16) []byte` and `syntax.WriteUint32(dst, v) []byte` â€” append-style free functions â€” and calls them literally in the code bodies for `mlsKdfLabel`, `RefHash`, `mlsSignContent` and `mlsEncryptContext`. p1 produces no `WriteVarVec` at all, and its `WriteUint16`/`WriteUint32` are methods on `*Writer` that return nothing. Nothing in any plan produces the consumed API, so every RFC 9420 Â§5.1 labelled preimage in p2 â€” the bytes MLS signs over â€” fails to compile. p2 has no reconciliation amendment.",
        "fix":  "Rewrite p2\u0027s four label encoders against p1\u0027s landed API: `w := syntax.NewWriter(); w.WriteOpaque([]byte(MlsLabelPrefix+label)); w.WriteOpaque(context); bs, err := w.Bytes()`. Replace the two rows of p2\u0027s \"needs from other plans\" table with `func NewWriter() *Writer`, `func (self *Writer) WriteOpaque(bs []byte)`, `func (self *Writer) WriteUint16(v uint16)`, `func (self *Writer) WriteUint32(v uint32)`, `func (self *Writer) Bytes() ([]byte, error)`, and keep p2\u0027s Task 12 boundary-conformance test pinning the three varint width transitions against `WriteOpaque`."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p4-key-schedule.md Â§\"Interface summary â€” what this plan consumes\" L273â€“300 and Task 1 compile assertions (L453) vs p1-syntax-codec.md Tasks 3, 4, 8, 13",
        "problem":  "p4 pins, as compile-time assertions in Task 1, `(*syntax.Writer).WriteOpaque(v []byte) error` and `(*syntax.Writer).WriteBytes(v []byte)`. p1 produces `WriteOpaque(bs []byte)` with no return value and names the raw writer `WriteRaw`, not `WriteBytes`. p4 also pins `ParseExtensions(r *syntax.Reader) ([]Extension, error)` and `MarshalExtensions(w *syntax.Writer, extensions []Extension) error` as coming from the wave-1 Syntax plan; p1 produces no `Extension` type or extension codec whatsoever. Task 1, whose declared purpose is to fail fast on drift, fails on four symbols at once â€” and `ParseExtensions`/`Extension` are actually produced by p5 (wave 2), which is p4\u0027s own wave, not wave 1.",
        "fix":  "In p4\u0027s consumed-interface block: change `WriteOpaque(v []byte) error` to `WriteOpaque(bs []byte)` (drop the error, surface it via `Bytes()`), change `WriteBytes` to `WriteRaw`, and move the `Extension`/`ExtensionType`/extension-codec block out of the \"From Syntax and codec (wave 1)\" section into a new \"From TreeKEM (wave 2)\" section with p5\u0027s actual signatures: `func MarshalExtensions(exts []Extension) ([]byte, error)` and `func UnmarshalExtensions(r *syntax.Reader) ([]Extension, error)`. Then rewrite `GroupContext.Marshal`/`ParseGroupContext` (Tasks 3â€“4) against those, and sequence p5 Task 3 before p4 Task 3."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p6-framing.md Â§\"Interfaces consumed from other plans\" L123â€“157 vs p1-syntax-codec.md Tasks 2, 3, 4, 10, 12, 13",
        "problem":  "p6 consumes a fifth incompatible spelling of `connect/mls/syntax`: `WriteOpaque(b []byte) error` (p1: no error), `Bytes() []byte` (p1: `([]byte, error)` â€” sticky error), `(*Writer).WriteVector(n int, each func(w *Writer, i int) error) error` and `(*Reader).ReadVector(each func(r *Reader) error) error` as methods (p1: free generics `WriteVector[T]`/`ReadVector[T]` taking a typed slice), `(*Reader).Rest() []byte` (no producer at all), `(*Reader).Finish() error` (p1: `Done()`), and `syntax.ErrNonMinimalLength` (p1: `ErrVarintNotMinimal`). Every codec in p6 â€” `Sender`, `FramedContent`, `PrivateMessage`, `MLSMessage` â€” is written against these. p6 states \"fix the call site â€” do not add an adapter layer\", so the mismatch is a real rewrite obligation that no task carries.",
        "fix":  "Add an Amendment section to p6 mirroring p5\u0027s Amendment A.1: `WriteOpaque(b) error` â†’ `w.WriteOpaque(b)` with the error taken at `Bytes()`; `Bytes() []byte` â†’ `bs, err := w.Bytes()`; `Finish()` â†’ `Done()`; `Rest()` â†’ `ReadRaw(r.Remaining())`; `ErrNonMinimalLength` â†’ `ErrVarintNotMinimal`; and replace the two `WriteVector`/`ReadVector` method calls with p1\u0027s `syntax.WriteVector(w, items, encodeOne)` / `syntax.ReadVector(r, decodeOne)` free generics, or with `ReadSub()` where the element type is heterogeneous."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p7-group-lifecycle.md Â§\"Interfaces consumed from other plans\" L56â€“67, and every struct in Tasks 2â€“20 (e.g. L1684â€“1691, 1711â€“1735, 1789â€“1798, 2085â€“2100, 2110â€“2136, 2216â€“2227) vs p1-syntax-codec.md Task 13",
        "problem":  "p7 assumes a reflective, struct-tag TLS codec: `syntax.Marshal(value any) ([]byte, error)`, `syntax.Unmarshal(data []byte, value any) (int, error)`, `type Marshaler interface{ MarshalTLS() ([]byte, error) }`, `type Unmarshaler interface{ UnmarshalTLS(data []byte) (int, error) }`, plus `tls:\"head=varint\"`, `tls:\"optional\"` and `tls:\"omit\"` field tags. p1 produces an interface-driven, hand-written codec: `Marshal(v Marshaler) ([]byte, error)`, `Unmarshal(bs []byte, v Unmarshaler) error`, `MarshalMLS(w *Writer)`, `UnmarshalMLS(r *Reader) error`, and no reflection and no struct tags anywhere. Every declared type in p7 â€” `GroupPolicyExtension`, `Proposal`, `ProposalOrRef`, `Commit`, `GroupInfo`, `GroupSecrets`, `Welcome`, `groupStateBlob` â€” carries `tls:` tags that p1\u0027s codec will silently ignore, producing wrong wire bytes rather than a compile error. This is the highest-consequence mismatch in the set: MLS signs over serialized forms, and `Welcome`/`GroupInfo` bytes decide whether joiners interop.",
        "fix":  "Rewrite p7\u0027s consumed-syntax block to p1\u0027s contract and convert every p7 struct to explicit `MarshalMLS(w *syntax.Writer)` / `UnmarshalMLS(r *syntax.Reader) error` methods, deleting all `tls:` tags â€” matching the convention p5 and p6 use. Alternatively, if the reflective codec is genuinely wanted, add it to p1 as a new task (`syntax/reflect.go`) with its own fuzz corpus and byte-exactness proof â€” but do not run two codecs, because Spec A\u0027s \"one codec, one fuzz corpus\" is what makes Gate 4 property 2 meaningful."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p8-validation-interop.md Â§\"Interfaces consumed by this plan\" L249â€“261 and Task 8 (L1836) vs p1-syntax-codec.md Tasks 2, 5, 6, 13",
        "problem":  "p8 consumes `syntax.Marshal(v any)`, `syntax.Unmarshal(b []byte, v any) error`, free functions `syntax.ReadVarint(b []byte) (uint64, int, error)` and `syntax.WriteVarint(dst []byte, value uint64) []byte`, plus `ErrNonMinimalLength` and `ErrVectorTooLong`. p1 produces `Marshal(v Marshaler)`, `Unmarshal(bs, v Unmarshaler) error`, methods `(*Reader).ReadVarint() (uint32, error)` and `(*Writer).WriteVarint(v uint32)`, and errors named `ErrVarintNotMinimal` and `ErrLengthExceedsMax`. p8 Task 8 â€” the family-16 runner that is Gate 1\u0027s first row â€” is written entirely against the free-function form and will not compile.",
        "fix":  "Rewrite p8\u0027s consumed-syntax block and Task 8 against p1: `r := syntax.NewReader(bs); v, err := r.ReadVarint()` and `w := syntax.NewWriter(); w.WriteVarint(v); bs, err := w.Bytes()`; rename `ErrNonMinimalLength` â†’ `ErrVarintNotMinimal`, `ErrVectorTooLong` â†’ `ErrLengthExceedsMax`. Note the varint is `uint32`-valued (`MaxVarint = 1\u003c\u003c30 - 1`), not `uint64`, so the family-16 case struct must decode into `uint32`."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p5-treekem.md L329â€“436 (MarshalTo/Marshal), p6-framing.md L240â€“303 (MarshalMLS ... error), p7-group-lifecycle.md L1796â€“1797 etc. (MarshalTLS), all vs p1-syntax-codec.md Task 13 `type Codec` and Task 14 `CheckRoundTrip`",
        "problem":  "Four different codec method-sets are declared for structures in one Go package `mls`: p1\u0027s `MarshalMLS(w *Writer)` with no error return, p6\u0027s `MarshalMLS(w *syntax.Writer) error` with one, p5\u0027s `MarshalTo(w) error` + `Marshal() ([]byte, error)` + free `UnmarshalX(r)` constructors, and p7\u0027s `MarshalTLS()/UnmarshalTLS()`. p1 Task 14 pins `CheckRoundTrip[T any, PT interface{ *T; Codec }](bs []byte) error` and states that all nine Gate 4 fuzz targets call it as `CheckRoundTrip[KeyPackage, *KeyPackage](bs)`. No structure produced by any plan satisfies `syntax.Codec`, because none of them has `MarshalMLS(w *Writer)` with p1\u0027s exact signature â€” so the shared round-trip property, which is the whole of Gate 4 property 2, cannot be instantiated on a single type.",
        "fix":  "Pick one convention and state it in every plan\u0027s global constraints. Recommended: p6\u0027s `MarshalMLS(w *syntax.Writer) error` / `UnmarshalMLS(r *syntax.Reader) error`, and change p1\u0027s `Marshaler` to `MarshalMLS(w *Writer) error` so `Marshal` propagates both the returned error and the sticky one. Then p5\u0027s `MarshalTo` becomes `MarshalMLS`, p5\u0027s `UnmarshalX(r) (X, error)` free functions gain thin `func (self *X) UnmarshalMLS(r *syntax.Reader) error` wrappers so `CheckRoundTrip` can name them, and p7 drops `MarshalTLS`/`UnmarshalTLS` entirely."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p5-treekem.md L209â€“226 and L7001; p7-group-lifecycle.md L103, L132â€“141; p6-framing.md L186â€“188, L3084 (Task 12 Consumes); p8-validation-interop.md L396â€“397, L3662, L5317",
        "problem":  "`KeyPackage` is consumed by four plans and produced by none. p5 lists it under \"mls/proposal.go, mls/key_package.go â€” Group lifecycle plan (wave 4)\". p7 lists it under \"From the TreeKEM plan (wave 2)\". p6 lists it under \"the key-package / leaf-node / extension / key-schedule plans\". p8 lists `NewKeyPackage`, `ParseKeyPackage`, `EncodeKeyPackage` under Group lifecycle. p7\u0027s own File Structure table has no `key_package.go` row and none of its 21 Produces blocks contains `KeyPackage`, `NewKeyPackage`, `(*KeyPackage).Ref` or `(*KeyPackage).Validate`. So the type carrying every joiner\u0027s init key, leaf node and signature â€” required by `Add`, `Welcome`, `MLSMessage`, vector families 8 and 12, the `key_package` codec kind of the differential oracle, and the interop harness\u0027s `CreateKeyPackage` RPC â€” is unowned work that nobody discovers until integration.",
        "fix":  "Add `connect/mls/key_package.go` to p7\u0027s File Structure and insert a task between p7 Task 4 and Task 5 producing: `type KeyPackage struct{ Version ProtocolVersion; CipherSuite CipherSuite; InitKey HpkePublicKey; LeafNode LeafNode; Extensions []Extension; Signature []byte }`, `func NewKeyPackage(crypto CryptoProvider, suite CipherSuite, cred Credential, caps Capabilities, exts []Extension) (*KeyPackage, HpkePrivateKey, HpkePrivateKey, error)`, `MarshalMLS`/`UnmarshalMLS`, `func (self *KeyPackage) Ref(crypto CryptoProvider) ([]byte, error)` delegating to p2\u0027s `MakeKeyPackageRef`, and `func (self *KeyPackage) Validate(crypto CryptoProvider, suite CipherSuite, now time.Time) error` delegating to p5\u0027s `LeafNode.Validate` with `ExpectedSource = LeafNodeSourceKeyPackage`. Update the Consumes blocks in p5 Task 28, p6 Tasks 12/14/17 and p8 Tasks 11/17 to name that task."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p7-group-lifecycle.md L306â€“315 (\"From the Validation and interop harness plan\") and L3981, 4198, 4450, 4945, 6379, 7080, 8763, 8846; p8-validation-interop.md L392 (\"From Group lifecycle (wave 4): type Profile struct { /* profile.go */ }\"), L3633, L5318; p2 L126; p3 L2933; p4 L76",
        "problem":  "`Profile` is a circular attribution. p2, p3, p4 and p7 all say the Validation and interop harness plan owns `connect/mls/profile.go`; p8 lists `Profile` in its *consumed* block, attributed to the Group lifecycle plan. Neither plan\u0027s tasks produce it. So `Profile`, `DefaultProfile()`, `CheckVersion`, `CheckProposalType`, `CheckGroupExtension`, `CheckLeafExtension`, `CheckCiphersuiteForCreate`, `CheckWireFormat` and `Profile.AllowPublicMessage` have no owner, while `GroupConfig.Profile` is a required field of `NewGroup`, `DefaultProfile()` is called at eight sites in p7 and in p8\u0027s forge, and the v1 narrow-profile refusals (Gate 3\u0027s 240â€“246 and 401â€“403 rows) run through it.",
        "fix":  "Assign `profile.go` explicitly to p8 and add a Phase A task producing the `Profile` type, `DefaultProfile()`, the seven `Check*` methods and the `AllowPublicMessage` relaxation p7 Task 21 needs for the passive-client vectors. Change p8\u0027s consumed block to drop `type Profile struct`, and change p7 L306 to reference that task number. Whichever plan takes it, only one may â€” and Spec A Â§2.2 already names `profile.go` alongside `errors.go` and `validation.go`, which p8 owns."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p5-treekem.md L247â€“260, L296â€“303 vs p6-framing.md L205â€“206, L3084 (Task 12) and L3561 (Task 13) vs p7-group-lifecycle.md L1680â€“1690, L1699, L1744, L1769â€“1798, L1812â€“1823, L3918â€“3924",
        "problem":  "Multiple types are declared by two or three plans in the same Go package `mls`, which is a redeclaration compile error, not a merge nuisance. `ProtocolVersion` and `ProtocolVersionMLS10`: p5 Task 3 and p6 Task 1. `ProposalType` and its constants: p5 Task 3, p6 Task 12, p7 Task 5 â€” three producers. `Proposal`, `Add`, `Update`, `Remove`, `GroupContextExtensions`, `ProposalOrRefType`, `ProposalOrRef`, `Commit`: p6 Tasks 12â€“13 and p7 Tasks 5, 6, 9, with incompatible shapes (p6\u0027s `Proposal` keeps `UnknownBody` for GREASE and accepts all seven types; p7\u0027s has no arm for psk/reinit/external_init). `LeafKeysExtension`, `AlgIdXwing`, the 1216-byte constant (`XwingPublicKeyLen` in p5, `XwingPublicKeySize` in p7) and `ExtensionTypeUrmessageLeafKeys`/`GroupPolicy`/`OwnerSuccessor`: p5 Tasks 3â€“4 and p7 Tasks 2â€“4.",
        "fix":  "Assign each type to exactly one plan and delete the other declaration: `ProtocolVersion`/`ProtocolVersionMLS10` and all `ExtensionType` constants â†’ p5 Task 3 (earliest wave); `LeafKeysExtension` and `AlgIdXwing`/`XwingPublicKeyLen` â†’ p5 Task 4, with p7 Task 2 reduced to the `LeafKeysOf(leaf)` accessor and the 0xF002 policy checks; `ProposalType` + `Proposal` + `ProposalOrRef` + `Commit` codecs â†’ p6 Tasks 12â€“13 (the `messages` gate needs the permissive codec), with p7 Task 5 reduced to `ParseProposal` (the profile gate) and p7 Tasks 6/9 to `CachedProposal`/`ProposalList`/`ProposalCache`/`CommitPathRequired`. Remove `ProposalType` from p5\u0027s produced list and have p5 consume it from p6, or move it to p5 and have p6 consume it â€” but name one owner."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p6-framing.md Task 1 (L1313â€“1322) vs p8-validation-interop.md Task 2 (L2320â€“2329) vs p2-crypto-primitives.md Task 1 (L236â€“246)",
        "problem":  "Ten error sentinels are declared by both p6 Task 1 (`errors_framing.go`) and p8 Task 2 (`errors.go`): `ErrWrongGroupId`, `ErrWrongEpoch`, `ErrBlankSenderLeaf`, `ErrApplicationMustBeCiphertext`, `ErrDecryptFailed`, `ErrMissingMembershipTag`, `ErrBadMembershipTag`, `ErrMissingConfirmationTag`, `ErrBadSignature`, `ErrNonZeroPadding`. `ErrBadSignature` is declared a third time by p2 Task 1 (`crypto_errors.go`). All three files are `package mls`, so this is `ErrBadSignature redeclared in this block`. p6 justifies the separate file as \"so this plan and the wave-1 validation plan never edit the same file\", which prevents a merge conflict but guarantees a compile error instead.",
        "fix":  "p8 Task 2 owns the ValSem sentinel set; delete those ten names from p6 Task 1\u0027s Produces block and have `errors_framing.go` hold only the non-ValSem structural errors (`ErrUnknownWireFormat`, `ErrUnsupportedVersion`, `ErrUnknownContentType`, `ErrUnknownSenderType`, `ErrContentArmMismatch`, `ErrMissingGroupContext`, `ErrUnexpectedGroupContext`, `ErrWireFormatMismatch`, `ErrSenderNotMember`, `ErrInvalidPaddingSize`). Rename p2\u0027s crypto-layer signature error to `ErrCryptoBadSignature` (or have p8\u0027s `ErrBadSignature` wrap it), and add a note to p6 Task 1 that it must be executed after p8 Task 2, which is wave 1 and therefore already available."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p6-framing.md L174â€“184 and Task 11 (L2721â€“2486, `MessageKeySource`) vs p4-key-schedule.md Task 23 produced surface (L4597â€“4650, interfaces.txt L940â€“958)",
        "problem":  "p6 requires `*SecretTree` to satisfy `MessageKeySource` with exactly `NextMessageKey(contentType ContentType, leaf LeafIndex) (key, nonce []byte, generation uint32, err error)`, `MessageKey(contentType ContentType, leaf LeafIndex, generation uint32) (key, nonce []byte, err error)` and `EraseMessageKey(contentType ContentType, leaf LeafIndex, generation uint32)`. p4 produces `NextSenderKey(leaf LeafIndex, kind RatchetType) (generation uint32, key, nonce []byte, err error)`, `ReceiverKey(leaf LeafIndex, kind RatchetType, generation uint32) (key, nonce []byte, err error)` and `SenderGeneration`. Different names, different parameter order, a different discriminant type (`RatchetType` vs `ContentType`), a different return order, and `EraseMessageKey` â€” the forward-secrecy erase of a consumed key â€” has no producer at all. `SealPrivateMessage`/`OpenPrivateMessage`, the whole encrypt path, cannot be called.",
        "fix":  "Add a task to p4 producing the three `MessageKeySource` methods on `*SecretTree` with p6\u0027s exact signatures, mapping `ContentTypeApplication` â†’ `RatchetApplication` and `ContentTypeProposal`/`ContentTypeCommit` â†’ `RatchetHandshake`, and implementing `EraseMessageKey` against the skipped-key window p4 Task 23 already owns; keep `NextSenderKey`/`ReceiverKey` as the internal form. Also reconcile the constructor: p4 gives `NewSecretTree(crypto, leafCount LeafIndex, encryptionSecret []byte) (*SecretTree, error)`, p6 Task 16 expects `(crypto, groupSize uint32, encryptionSecret)` and p7 expects `(crypto, size TreeSize, encryptionSecret) *SecretTree` with no error â€” settle on p4\u0027s, taking `LeafCount` (p3\u0027s type) rather than `LeafIndex`."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p7-group-lifecycle.md L186â€“220 (\"From the Key schedule and secret tree plan\") and Tasks 11, 13, 15, 16 vs p4-key-schedule.md L110â€“135 and Tasks 5â€“12",
        "problem":  "p7 consumes an epoch-secrets API that p4 does not produce: `DeriveEpochSecrets(crypto, initSecretPrev, commitSecret, pskSecret, groupContext []byte) (*EpochSecrets, error)`, `DeriveEpochSecretsFromJoiner(...)`, `DeriveEpochSecretsFromEpochSecret(crypto, epochSecret []byte) (*EpochSecrets, error)`, `EmptyPskSecret(crypto)`, `(*EpochSecrets).Export(...)`, `WelcomeKeyNonce(...) (key, nonce []byte)` with no error, and an `EpochSecrets` whose eleven fields (`JoinerSecret`, `WelcomeSecret`, `SenderDataSecret`, â€¦) are named differently from p4\u0027s nine (`SenderData`, `Encryption`, `Exporter`, â€¦). p4 produces a `*KeySchedule` object with `NewKeySchedule`/`NewKeyScheduleFromJoiner`. Beyond naming, `DeriveEpochSecretsFromEpochSecret` is a genuine functional gap: p7\u0027s `NewGroup` follows RFC 9420 Â§11 by sampling a fresh `epoch_secret` of size `KDF.Nh` for epoch 0, and p4 offers no entry point that starts from a raw `epoch_secret` â€” only from `init_secret + commit_secret` or from a `joiner_secret`. `EmptyPskSecret` also has no producer.",
        "fix":  "Add to p4: `func NewKeyScheduleFromEpochSecret(crypto CryptoProvider, epochSecret []byte, groupContext *GroupContext) (*KeySchedule, error)` (the Â§11 group-creation entry; `joiner_secret` and `welcome_secret` are undefined on that path and must return an error if asked for) and `func EmptyPskSecret(crypto CryptoProvider) []byte` = `PskSecret(crypto, nil)`. Then rewrite p7\u0027s consumed block to p4\u0027s actual names â€” `NewKeySchedule`, `NewKeyScheduleFromJoiner`, `(*KeySchedule).Secrets() *EpochSecrets`, `(*KeySchedule).Export`, `WelcomeKeyNonce(...) (key, nonce []byte, err error)` â€” and rename the `EpochSecrets` field references in p7 Tasks 11, 13, 15, 16, 18 to p4\u0027s spelling."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p7-group-lifecycle.md L103â€“184 (\"From the TreeKEM plan (wave 2)\") vs p5-treekem.md L243â€“487 (\"Interfaces produced by this plan\")",
        "problem":  "p7\u0027s entire TreeKEM consumption block names symbols p5 does not produce, or produces under a different name and shape. Gaps with no producer anywhere: `NewLeafNode`, `(*Capabilities).Supports(rc *RequiredCapabilities) error`, `(*RatchetTree).Size() TreeSize`, `NonBlankLeaves`, `RootHash`, `Encode`, `EncryptionKeyInUse`, `HasTrailingBlankNodes`, `MergeUpdatePathPublic`, `type PathSecrets` with `SecretAt`/`CommitSecret`, `PathSecretToNodeKeyPair`, `DerivePathSecretNext`. Renames: `ParseRatchetTree(crypto, data)` vs p5\u0027s `UnmarshalRatchetTree(data)`; `(*RatchetTree).LeafNode(i) (*LeafNode, bool)` vs p5\u0027s `Leaf(i) *LeafNode`; `NewRatchetTree(crypto)` vs p5\u0027s `NewRatchetTree()`; `CreateUpdatePath(...) (*UpdatePath, *PathSecrets, HpkePrivateKey, error)` vs p5\u0027s two-step `CreateUpdatePathSecrets` â†’ `EncryptUpdatePath`; `ApplyUpdatePath(...)` vs p5\u0027s `MergeUpdatePath` + `DecryptUpdatePath`. Signature changes: `LeafNode.Validate(crypto, groupId, leaf, source, exts, now)` vs p5\u0027s `Validate(ctx *LeafValidationContext)`; `RatchetTree.Validate(groupId, exts, now)` vs p5\u0027s `Validate(ctx *TreeValidationContext)`. p7 Tasks 8, 10, 11, 13, 15, 16, 18 and 19 are all written against the non-existent form.",
        "fix":  "Rewrite p7\u0027s TreeKEM consumption block verbatim from p5 L243â€“487 and adjust p7\u0027s call sites, since p7 itself states \"that plan wins\". The one place p5 must move is the ordering contract: p5\u0027s `CreateUpdatePathSecrets` mutates the tree before `EncryptUpdatePath` is called with the *post*-mutation GroupContext, so p7 Task 13 (`Group.Commit`) must be restructured as create â†’ TreeHash â†’ build GroupContext â†’ encrypt, not the single `CreateUpdatePath` call it currently assumes. Add the genuinely missing accessors to p5 Task 8: `Size()`, `NonBlankLeaves()`, `EncryptionKeyInUse(key)`, `HasTrailingBlankNodes()`."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p4-key-schedule.md L260â€“271 (Task 1 compile assertions) and Task 21 (L4022â€“4028); p7-group-lifecycle.md L91â€“101; p8-validation-interop.md L282â€“290 â€” all vs p3-tree-math.md L2857â€“2909",
        "problem":  "Three plans pin a tree-math contract that p3 does not produce, and p3 is the wave-1 plan every one of them says they depend on. p4 pins `NodeWidth(leafCount LeafIndex) NodeIndex`, `Root(leafCount LeafIndex) NodeIndex` and `Level(x NodeIndex) uint32` as compile-time assertions in Task 1; p3 produces `NodeWidth(n LeafCount) uint32`, `Root(n LeafCount) (NodeIndex, error)` and `Level` as a method on `NodeIndex`. p7 consumes `type TreeSize uint32`, `(self TreeSize) Root() NodeIndex`, `CommonAncestor(x, y LeafIndex)` and `DirectPath(x NodeIndex, size TreeSize) []NodeIndex` â€” `TreeSize` does not exist (p3 has `LeafCount`), `CommonAncestor` takes `NodeIndex` not `LeafIndex`, and `DirectPath` returns an error. p8 consumes `Root(leafCount uint32) NodeIndex`, `DirectPath(x LeafIndex, leafCount uint32)` and `Copath(x LeafIndex, leafCount uint32)` with no error. Only p5 reconciled this, in Amendment A.2.",
        "fix":  "Copy p5\u0027s Amendment A.2 table into p4, p7 and p8 verbatim: every `LeafIndex`/`uint32`/`TreeSize` count parameter becomes `LeafCount`, every `Root`/`Left`/`Right`/`Parent`/`Sibling`/`DirectPath`/`Copath` call becomes two-valued with an error, `Level(x)` becomes `x.Level()`, and `x.LeafIndex()` returns `(LeafIndex, error)`. Delete `TreeSize` from p7 and use `LeafCount`. Fix p4 Task 1\u0027s assertion block first, since its entire purpose is to catch exactly this drift and as written it catches it by failing."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p6-framing.md Task 14 (L3756â€“3760, `MLSMessage`) and Task 17 (L4552â€“4556, the `messages` gate) â€” wave 3 â€” consuming p7-group-lifecycle.md Tasks 13, 14 (`GroupInfo`, `Welcome`, `GroupSecrets`) and the unowned `KeyPackage` â€” wave 4",
        "problem":  "Wave-ordering violation. p6 is wave 3. `MLSMessage` is declared with `Welcome *Welcome`, `GroupInfo *GroupInfo` and `KeyPackage *KeyPackage` fields and p6 Task 14 states the arms are \"referenced by direct type, not through a registry\". `Welcome`, `GroupInfo` and `GroupSecrets` are produced by p7 Tasks 13â€“14 (wave 4), and `KeyPackage` by nobody. Because everything is one Go package, `package mls` cannot compile at the end of wave 3, so `ParseMLSMessage` â€” which p6 correctly identifies as \"the single entry point every byte off the wire passes through\" â€” is unavailable to the whole of wave 3, including p6\u0027s own Task 16 message-protection vector gate. p6 Task 17 partly acknowledges this (\"comment out that row only\") but Task 14\u0027s struct declaration cannot be commented out row-wise.",
        "fix":  "Move `GroupInfo`, `GroupInfoTBS`, `PathSecret`, `GroupSecrets`, `EncryptedGroupSecrets` and `Welcome` â€” the codecs only, not `BuildWelcome`/`JoinFromWelcome` â€” out of p7 Tasks 13â€“14 into a new wave-3 task in p6 alongside `proposal_types.go` and `commit_types.go`, using exactly the split rationale p6 already applies to `Proposal`/`Commit`. p7 then keeps the generation and processing logic and consumes the codecs. Alternatively re-label p6 as wave 4 and accept that the `messages` gate lands last â€” but that puts Gate 1\u0027s largest family behind the whole group lifecycle."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p8-validation-interop.md Phase A header (L504, \"wave 1; depends on nothing but connect/mls/syntax\") vs Task 10 (L2050, consumes `StateStore` from wave 4), Task 11 (L2394â€“2397, consumes `ParseKeyPackage`/`ParseProposal`/`ParseWelcome` from wave 4) and Task 12 (L2410, the nine fuzz targets)",
        "problem":  "p8\u0027s Phase A is declared wave 1 and \"depends on nothing but connect/mls/syntax\", but three of its twelve tasks consume wave-3 and wave-4 symbols. Task 10 (`memStore`) implements `mls.StateStore`, produced by p7 Task 11 (wave 4). Task 11 (`CodecFor`) requires the five decode/encode pairs, two of which (`KeyPackage`, `Welcome`) are wave 4, one (`MlsMessage`) is wave 3, and one (`Proposal`) is wave 3/4. Task 12 builds the nine Gate 4 fuzz targets on top of Task 11. Since Task 13 wires those test names into the per-commit CI jobs, the `fuzz-short` job is red from wave 1 to wave 4 â€” which trains everyone to ignore it.",
        "fix":  "Split Phase A: keep Tasks 1â€“9 and 13 (errata, `ValSemCode`/`ValidationError`, catalogue, layering, forbidden-crypto, vector pin, registry, family 16, generate-then-verify) in wave 1, and move Tasks 10, 11 and 12 into a new Phase Aâ€² marked wave 4, after `group.go`. Have Task 13\u0027s CI job register the `fuzz-short` target list as it grows rather than declaring all nine at wave 1, and state the same for the `vectors` job."
    },
    {
        "severity":  "MAJOR",
        "location":  "p8-validation-interop.md Task 7 (L1706â€“1779, `RegisterVectorFamily`, `vectorManifest`, `expectedPendingFamilies`) vs p1 Task 7, p2 Tasks 17â€“18, p3 Task 7, p4 Tasks 17/20/25, p5 Tasks 24/25, p6 Task 16, p7 Tasks 17/21",
        "problem":  "p8 Task 7 declares itself \"the contract every other slice-1 plan registers against\" and requires each owning plan to call `RegisterVectorFamily` from an `init()` in its own `*_kat_test.go`, with `expectedPendingFamilies` shrinking from `{1..15}` to empty as runners land. Not one of the other seven plans mentions `RegisterVectorFamily` anywhere â€” p3 goes further and states its vector test is \"self-contained: deliberately depends on no shared vector harness\". Every plan writes a standalone `TestXxxVectors`. The result is that `vectorManifest` keeps 15 nil `Verify` funcs forever, `TestVectorFamiliesVerify` runs zero families, and `TestVectorManifestIsComplete` passes vacuously â€” so Acceptance criterion 1 (\"all 16 RFC 9420 test-vector families pass\") has no gate behind it even when all 16 runners exist.",
        "fix":  "Add a step to each family-owning task instructing it to register: p2 Task 17 (family 2), p3 Task 7 (1), p4 Tasks 17/20/25 (5, 7, 3, 6), p5 Tasks 24/25/28 (10, 11, 9), p6 Tasks 16/17 (4, 12), p7 Tasks 17/21 (8, 13, 14, 15) â€” each adding `func init() { RegisterVectorFamily(VectorFamily{Number: N, ..., Verify: verifyFamilyN, Generate: generateFamilyN}) }` and deleting its number from `expectedPendingFamilies` in the same commit. For p3, which is in package `mls` already, this is a five-line addition; for p1, whose family-16 runner is in package `syntax`, see the family-16 finding."
    },
    {
        "severity":  "MAJOR",
        "location":  "p1-syntax-codec.md Task 7 (L1174â€“1308, family 16 in `package syntax`, `mls/syntax/vectors_test.go`) vs p8-validation-interop.md Task 8 (L1829â€“1842, family 16 in `package mls`, `mls/vectors_deserialization_kat_test.go`)",
        "problem":  "Family 16 (`deserialization.json`) is implemented twice, in two packages, by two plans, both in wave 1. p1\u0027s version lives in `package syntax` and reads `../testdata/vectors/deserialization.json`; p8\u0027s lives in `package mls`, is the one that clears family 16 from `expectedPendingFamilies`, and is written against free `syntax.ReadVarint`/`syntax.WriteVarint` functions that do not exist. Two runners over one file will drift, and the manifest gate can only see one of them.",
        "fix":  "Keep p1\u0027s runner as the package-local codec test (rename it `TestSyntaxDeserializationVectors` so it is clearly not the gate), and make p8 Task 8 the registered family-16 runner, rewritten against p1\u0027s `Reader.ReadVarint`/`Writer.WriteVarint` methods. Alternatively delete p8 Task 8 and have p1 register family 16 through a tiny `mls/vectors_syntax_kat_test.go` in `package mls` that imports `mls/syntax` â€” but state which, because as written both plans commit a file for the same family."
    },
    {
        "severity":  "MAJOR",
        "location":  "p8-validation-interop.md Task 6 (L1476, creates all 16 `testdata/vectors/*.json` plus `VECTORS.sha256` plus `connect/mls/PINS.md`) vs p1 Task 7, p2 Tasks 9/17/20, p3 Task 1, p5 Task 1, p7 Tasks 17/21 (each also vendors files and Creates `connect/mls/testdata/vectors/PINS.md`); Spec A Â§4.2.4 and p7 L341 name a third file, `connect/mls/interop/PINS.md`",
        "problem":  "Vector provenance has two competing mechanisms and four writers. p8 Task 6 vendors all sixteen files and pins them by digest in `testdata/vectors/VECTORS.sha256`, with the commit recorded in `connect/mls/PINS.md`. p1, p2, p3, p5 and p7 each independently vendor their own subset and each \"Create\" `connect/mls/testdata/vectors/PINS.md` â€” p1 explicitly says \"Do not create a second pin file\", p3 says \"Create (append if it already exists)\", p5 says \"Create\". Whichever plan runs second overwrites the first\u0027s rows, and p8\u0027s `TestVectorFilesArePinned` reads a manifest file none of the other plans writes. p2 additionally pins digests as Go constants inside its test files, a third mechanism.",
        "fix":  "Make p8 Task 6 the single vendoring task for all sixteen mlswg files and `VECTORS.sha256`, executed first in wave 1, and delete the vendoring steps from p1 Task 7, p3 Task 1, p5 Task 1 and p7 Tasks 17/21 (they keep only their runners). Keep p2\u0027s separately-sourced files (`hpke-rfc9180-x25519.json` from the CFRG repo, `xwing-draft10.json` from the draft repo) in p2, but have them append rows to `VECTORS.sha256` rather than to a second `PINS.md`. Settle on exactly one path for the mlswg commit pin â€” `connect/mls/PINS.md` â€” and have `connect/mls/interop/PINS.md` hold only the OpenMLS and proto pins."
    },
    {
        "severity":  "MAJOR",
        "location":  "p2-crypto-primitives.md L142â€“143 vs p4 L227â€“228, p5 L115, p6 L165, p7 L73, p8 L267â€“268",
        "problem":  "The ciphersuite constants have three spellings and five plans consume names p2 does not produce. p2 produces `CipherSuiteX25519AesGcm128Sha256Ed25519` (0x0001) and `CipherSuiteX25519ChaCha20Sha256Ed25519` (0x0003). p4, p5, p6, p7 and p8 all consume `CipherSuiteX25519ChaCha20SHA256Ed25519` â€” capital SHA. For 0x0001, p4 consumes `CipherSuiteX25519AES128SHA256Ed25519` and p8 consumes `CipherSuiteX25519AES128GCMSHA256Ed25519`. Since the 0x0003 constant is named in the global constraints of every plan and appears in most task bodies, this breaks the build in five places at once.",
        "fix":  "Adopt the five-plan majority spelling in p2 Task 3: `CipherSuiteX25519ChaCha20SHA256Ed25519 CipherSuite = 0x0003` and `CipherSuiteX25519AES128GCMSHA256Ed25519 CipherSuite = 0x0001`, and fix p4\u0027s `CipherSuiteX25519AES128SHA256Ed25519` reference to the same. Note p2 Task 3\u0027s registry test asserts the constant names, so update it in the same commit."
    },
    {
        "severity":  "MAJOR",
        "location":  "p7-group-lifecycle.md L222â€“288 (\"From the Framing and message protection plan\") vs p6-framing.md L306â€“309, Tasks 5, 7, 11, 14 and p4-key-schedule.md Task 19",
        "problem":  "p7\u0027s framing consumption block names symbols p6 does not produce and attributes two of p4\u0027s to p6. Renames with no producer: `SignFramedContent` (p6: `SignAuthenticatedContent`), `ProtectPrivate` (p6: `SealPrivateMessage`), `UnprotectPrivate` (p6: `OpenPrivateMessage`), `(*AuthenticatedContent).VerifySignature` (p6: free `VerifyAuthenticatedContent`), `(*MLSMessage).Marshal` (p6: `MarshalMLSMessage`), `(*AuthenticatedContent).ProposalRef(crypto)` (no producer; p2 has free `MakeProposalRef`). Misattributed: `ConfirmedTranscriptHash` and `InterimTranscriptHash` are p4\u0027s, not p6\u0027s, and p4 deliberately takes `confirmedTranscriptHashInput []byte` rather than p7\u0027s `content *AuthenticatedContent` so that no framing type crosses into `transcript.go`. Signature shape: every p6 entry point takes `groupContext []byte` â€” a deliberate decision p6 documents at length â€” while p7 passes `*GroupContext`.",
        "fix":  "Rewrite p7\u0027s framing block from p6 L306â€“309 and p4 L167â€“177: use p6\u0027s names, pass `groupContext []byte` obtained from `(*GroupContext).Marshal()` at every call site in p7 Tasks 12, 13, 18, and call `ConfirmedTranscriptHash(crypto, interimBefore, authContent.ConfirmedTranscriptHashInput())` to bridge p6\u0027s serializer to p4\u0027s arithmetic. Add `func (self *AuthenticatedContent) ProposalRef(crypto CryptoProvider) (ProposalRef, error)` to p6 Task 4 as a one-line wrapper over `MakeProposalRef`, since p7 Task 6\u0027s `ProposalCache.Store` depends on it."
    },
    {
        "severity":  "MAJOR",
        "location":  "p7-group-lifecycle.md L80â€“86 vs p2-crypto-primitives.md L232â€“233 and p5-treekem.md L421â€“424",
        "problem":  "p7 consumes `type HPKECiphertext struct` with `tls:` tags from the Crypto plan, plus `EncryptWithLabel(...) (*HPKECiphertext, error)` and `DecryptWithLabel(crypto, priv, label, context, ct *HPKECiphertext) ([]byte, error)`. p2 produces neither the type nor those signatures â€” its `EncryptWithLabel` returns `(kemOutput []byte, ciphertext []byte, err error)` and `DecryptWithLabel` takes the two byte slices separately. The struct is produced by p5 Task 19 as `HpkeCiphertext` (lowercase pke), which p8 correctly consumes. So p7 has both the wrong owner and the wrong spelling, and p7 Task 15 (`BuildWelcome`) and Task 16 (`JoinFromWelcome`) â€” the RFC Â§12.4.3.1 path â€” are written against it.",
        "fix":  "Change p7\u0027s consumption to `HpkeCiphertext` from p5 Task 19, and rewrite p7 Tasks 15/16 to call p2\u0027s `EncryptWithLabel(crypto, initKey, \"Welcome\", encryptedGroupInfo, groupSecrets) (kemOutput, ciphertext []byte, err error)` and assemble the struct at the call site. Alternatively add convenience wrappers `EncryptWithLabelCT`/`DecryptWithLabelCT` to p5 Task 19 next to the type they return â€” but not to p2, which must stay free of TreeKEM types."
    },
    {
        "severity":  "MAJOR",
        "location":  "p8-validation-interop.md Task 11 (L2394â€“2402) vs p5 L266â€“270, p6 L302â€“303, p7 Task 14, and the unowned `KeyPackage`",
        "problem":  "p8\u0027s codec table â€” the five decode/encode pairs the Gate 4 fuzz targets and the OpenMLS oracle protocol share â€” names `ParseExtension`/`EncodeExtension`, `ParseKeyPackage`/`EncodeKeyPackage`, `ParseMLSMessage`/`EncodeMLSMessage`, `ParseProposal`/`EncodeProposal`, `ParseWelcome`/`EncodeWelcome`. Of these ten, only `ParseMLSMessage` and `ParseProposal` exist: p5 produces `UnmarshalExtension(r *syntax.Reader)` (reader-based, not byte-based) and no `EncodeExtension`; p6 produces `MarshalMLSMessage`, not `EncodeMLSMessage`; `ParseWelcome`/`EncodeWelcome` have no producer; `ParseKeyPackage`/`EncodeKeyPackage` inherit the unowned-`KeyPackage` problem. Since `CodecFor` is what both the fuzz targets and the differential oracle dispatch through, Acceptance criterion 4 has no callable surface.",
        "fix":  "Define the five pairs as byte-in/byte-out adapters inside p8 Task 11 itself, built from whatever each owning plan produces: `func(b []byte) (any, error) { e := Extension{}; return \u0026e, syntax.Unmarshal(b, \u0026e) }` and so on. That keeps the naming contract inside one plan and removes ten cross-plan name asks. Add the missing `Welcome` and `KeyPackage` `MarshalMLS`/`UnmarshalMLS` pairs to whichever plan takes those types."
    },
    {
        "severity":  "MAJOR",
        "location":  "p8-validation-interop.md L271 (`NewCryptoProviderWithRandom`), L272 (`RegisteredSuites`), L365â€“366 (`(*Group).sealFramedContentForTest`), L383 (`(*Group).ClearPendingCommit`), L4198 (`RatchetTreeExtension`) vs p2 L173/L225, p6 produced surface, p7 Task 19",
        "problem":  "Five symbols p8 consumes have no producer. `NewCryptoProviderWithRandom(suite, random io.Reader)` â€” p2 produces only `NewCryptoProvider(suite)`; p8 needs it so a failing ValSem test reproduces from a fixed seed, and p8\u0027s forge (Task 17) is built on it. `RegisteredSuites()` â€” p2 produces `Suites()`. `(*Group).sealFramedContentForTest` â€” the construction-bypass seam that all ten framing ValSem tests (002â€“011) depend on; p6 produces nothing like it and p8 correctly flags it as \"an ask on that plan\", but no p6 task takes the ask. `(*Group).ClearPendingCommit` appears in p7\u0027s file-structure prose but in no Produces block. `RatchetTreeExtension` (p8 Task 20) has no producer in any plan.",
        "fix":  "Add `func NewCryptoProviderWithRandom(suite CipherSuite, random io.Reader) (CryptoProvider, error)` to p2 Task 11 (the concrete type already holds a `Random(n int)` method, so this is a field), and rename `Suites` â†’ `RegisteredSuites` or fix p8. Add a step to p6 Task 7 producing the unexported `sealFramedContentForTest` seam in `framing.go` with p8\u0027s exact signature. Add `ClearPendingCommit` to p7 Task 19\u0027s Produces block. Replace `RatchetTreeExtension` in p8 Task 20 with `FindExtension(exts, ExtensionTypeRatchetTree)` plus `UnmarshalRatchetTree`, both of which p5 produces."
    },
    {
        "severity":  "MAJOR",
        "location":  "p8-validation-interop.md Task 2 (L2320â€“2329, the 46 sentinels) vs p5-treekem.md Amendment A.3 (L7370, L7382, `ErrProfileCredentialType`), p6-framing.md Task 12 (`ErrProfileReInit`), p7-group-lifecycle.md L303â€“304 and Task 5 (L1766)",
        "problem":  "p8\u0027s `errors.go` list is explicitly \"the 46 sentinels\", but five profile-refusal errors consumed by other plans are absent from it: `ErrProfileCredentialType` (p5\u0027s amended `credential.go` returns it on an x509 credential â€” the only thing enforcing BasicCredential-only at parse), `ErrProfileReInit` (p6 and p7 Task 5), `ErrProfileExternalSender`, `ErrProfileBranch` and `ErrProfileCiphersuite` (p7 L303â€“304). The narrow v1 profile is a global constraint of this slice â€” \"all parse-refused with typed errors\" â€” and five of the refusals have no error value to return.",
        "fix":  "Extend p8 Task 2\u0027s sentinel list to 51 with `ErrProfileCredentialType`, `ErrProfileReInit`, `ErrProfileExternalSender`, `ErrProfileBranch` and `ErrProfileCiphersuite`, and update the Task 3 catalogue-closure test\u0027s expected count. Map each to a ValSem code or mark it explicitly profile-only in `ValSemCatalogue`, so `TestValSemCatalogueIsClosed` does not start failing on the five new names."
    },
    {
        "severity":  "MAJOR",
        "location":  "p4-key-schedule.md Task 24 (L4930â€“4971, exported `SenderDataKeyNonce`) vs p6-framing.md Task 9 (L2437â€“2440, unexported `senderDataKeyNonce`)",
        "problem":  "The RFC 9420 Â§6.3.2 sender-data key/nonce derivation is implemented twice. p4 Task 24 produces exported `SenderDataKeyNonce(crypto, senderDataSecret, ciphertext) (key, nonce []byte, err error)` and states \"The Framing plan calls this... it lives here rather than in framing.go because secret-tree.json checks it\". p6 Task 9 independently produces unexported `senderDataKeyNonce(crypto, senderDataSecret, ciphertext) (key, nonce []byte)` with no error return, inside `framing_sender_data.go`. Two implementations of one derivation, only one of which is vector-tested, and the untested one is the one the encrypt/decrypt path actually calls. Both plans separately document the same `ciphertext_sample` short-ciphertext trap, which is exactly the bug a second copy will eventually get wrong.",
        "fix":  "Delete p6 Task 9\u0027s `senderDataKeyNonce` and have `sealSenderData`/`openSenderData` call p4\u0027s exported `SenderDataKeyNonce`, propagating its error. Keep p6\u0027s two short-ciphertext tests â€” they are worth having as regression tests against p4\u0027s implementation â€” and add a note to p4 Task 24 that `secret-tree.json` is the only vector coverage for this derivation."
    },
    {
        "severity":  "MAJOR",
        "location":  "p5-treekem.md Task 28 (L6996â€“7005) consuming `ProposalKind`, `Proposal{Kind, Add *KeyPackage, Update *LeafNode, Remove *LeafIndex}`, `ParseProposal` from wave 4 vs p7-group-lifecycle.md Task 5 (L1769â€“1798) and p6-framing.md Task 12",
        "problem":  "p5 Task 28 (vector family 9, tree-operations) is the plan\u0027s only wave-4 dependency and is written against a `Proposal` shape nobody produces: `type ProposalKind uint16` with `ProposalKindAdd/Update/Remove`, and a `Proposal` whose arms are `*KeyPackage`, `*LeafNode`, `*LeafIndex` directly. p7 produces `ProposalType` with `Add`/`Update`/`Remove` wrapper structs; p6 produces a third variant. So family 9 cannot be implemented as written even once wave 4 lands, and it is left as \"the one unchecked box\" â€” meaning a Gate 1 family silently never runs.",
        "fix":  "Rewrite p5 Task 28\u0027s Consumes block against whichever plan wins the `Proposal` ownership question: `ParseProposal(b []byte) (*Proposal, error)`, `Proposal.ProposalType`, `Proposal.Add.KeyPackage`, `Proposal.Update.LeafNode`, `Proposal.Remove.Removed`. Since p5 is wave 2 and this is wave 4, move Task 28 out of p5 into the wave-4 plan that owns `Proposal`, so the family has an owner in the wave where it can actually be executed, and register it there."
    },
    {
        "severity":  "MINOR",
        "location":  "p2-crypto-primitives.md Task 1 (L94, `connect/go.mod` â†’ `go 1.26.5`) vs p1-syntax-codec.md Task 1 Step 5 (L189â€“200, `go 1.26.3` + `toolchain go1.26.5`) and p3-tree-math.md L21 / L2947â€“2949",
        "problem":  "Two wave-1 plans modify `connect/go.mod` in their first task with different intents. p1 deliberately leaves the `go` directive at 1.26.3 and adds only `toolchain go1.26.5`, reasoning that raising the directive raises the language-version floor for all of `connect`, which is out of scope for this slice. p2 Task 1 raises the directive to 1.26.5. p3 notes the discrepancy and asks that it \"be resolved deliberately by whoever cuts beta/message\". Whichever runs second either conflicts or silently reverses the other\u0027s decision.",
        "fix":  "Adopt p1\u0027s form â€” `go 1.26.3` plus `toolchain go1.26.5` â€” and change p2 Task 1\u0027s File row and step to \"Modify: connect/go.mod â€” add `toolchain go1.26.5` if the Syntax plan has not already\", with a `git diff --exit-code go.mod` guard. Delete the open question from p3 L2947â€“2949 once decided."
    },
    {
        "severity":  "MINOR",
        "location":  "p4-key-schedule.md Task 13 (`PreSharedKeyId`) vs p6-framing.md Task 12 (L3490, `PreSharedKeyID`) and p8-validation-interop.md Task 22 (L4515, `PreSharedKeyID`)",
        "problem":  "p4 produces `PreSharedKeyId` (Go-idiomatic `Id`, and p4 explicitly warns other plans \"must not redefine this type\"); p6 Task 12 and p8 Task 22 both consume `PreSharedKeyID`. One capital letter, one undefined identifier, in the `messages` vector family codec and in the ValSem401â€“403 tests.",
        "fix":  "Change p6 Task 12 and p8 Task 22 to `PreSharedKeyId`, matching p4 and `CODESTYLE.md`\u0027s usage+type naming."
    },
    {
        "severity":  "MINOR",
        "location":  "p7-group-lifecycle.md L63 (`const MaxVectorLength uint64`) and p8-validation-interop.md L256 (`const MaxVectorLength = 1 \u003c\u003c 20`) vs p1-syntax-codec.md Task 2 (L222, `const MaxVectorLength int = 1 \u003c\u003c 20`)",
        "problem":  "p1 types the limit constants as `int` so they can be passed directly to `MarshalLimit`/`UnmarshalLimit`/`NewReaderLimit`. p7 declares the consumed constant as `uint64`, p8 as untyped. A `uint64` constant will not satisfy `UnmarshalLimit(bs, v, maxVectorLength int)` without a conversion, and the two ratchet-tree call sites p1 names (`tree_sync.go`, and any `GroupInfo`/`Welcome` decode carrying a tree) are exactly where the conversion would be forgotten.",
        "fix":  "Change p7 L63 to `const MaxVectorLength int` and add `const MaxRatchetTreeLength int` next to it, since p7\u0027s `JoinFromWelcome` and `LoadGroup` both decode ratchet trees and must use `UnmarshalLimit(bs, v, syntax.MaxRatchetTreeLength)` rather than plain `Unmarshal`."
    },
    {
        "severity":  "MINOR",
        "location":  "p4-key-schedule.md Task 16 (`ksHex`, `ksLoadVectors`, `ksImplementedSuite`), p6-framing.md Task 16 (L4569â€“4571, `hexBytes`), p8-validation-interop.md Task 7 (`MustHex`, `HexOf`, `LoadVectorFile`) â€” all `package mls`",
        "problem":  "Three vector-loading helper sets are declared in one package. p4 prefixes its with `ks` specifically to dodge a collision and calls collapsing them \"a follow-up after both waves land\"; p6 declares `hexBytes` with an instruction to delete it if a wave-1 harness already declares one; p8 declares the canonical `MustHex`/`HexOf`/`LoadVectorFile` at Task 7, which is wave 1 and therefore lands before both. Three parallel hex decoders over one corpus is how two of them end up disagreeing about the empty-string case.",
        "fix":  "Have p4 Task 16 and p6 Task 16 consume p8 Task 7\u0027s `MustHex`/`HexOf`/`LoadVectorFile` directly and produce no helpers of their own â€” p8 is wave 1, so they are available. Keep only genuinely family-specific helpers (`ksImplementedSuite` is one) and rename it `implementedSuite` once it is the only copy."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p6-framing Task 18 (connect/mls/framing_fuzz_test.go, lines 4900-4996) vs p8-validation-interop Task 12 (connect/mls/syntax_fuzz_test.go, lines 2456-2464)",
        "problem":  "Both plans declare FuzzMlsMessageDecode, FuzzMlsMessageDecodeBytes, FuzzProposalDecode and FuzzProposalDecodeBytes as top-level funcs in package mls. Four duplicate symbols; the package will not build, so acceptance criterion 4 has zero passing targets. P6 says the other five \u0027belong to the extension, key-package and welcome plans\u0027 while P8 claims all nine.",
        "fix":  "Give all nine Gate-4 targets to p8 Task 12 exclusively (it already has the shared codec table and the oracle hook that property 3 needs). Delete the four target functions from p6 Task 18, keeping only TestFramingUsesConstantTimeComparison and the corpus seeding, and change p6\u0027s Produces block to \u0027seed corpus for mls_message and proposal targets\u0027. Add a note in p8 Task 12 that the mls_message/proposal Decode/Encode halves come from the framing plan."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p5-treekem Tasks 25/26 (lines 5185, 5369, 5403, 2780), p6-framing Task 7 (line 1799), p8-validation-interop Tasks 18/20 (lines 3876, 4234, 4242, 4250, 4311)",
        "problem":  "Five test functions are declared verbatim twice in package mls: TestValSem005_ApplicationMustBeCiphertext (p6 + p8), TestValSem202_PathLength, TestValSem203_PathDecrypt, TestValSem204_PathKeyMismatch and TestValSem300_TrailingBlankNodes (p5 + p8). Duplicate symbols break the build for the whole ValSem suite, i.e. acceptance criterion 3.",
        "fix":  "Spec A Â§4.3 fixes the name TestValSemNNN_\u003cslug\u003e and fixes the files (validation_framing_test.go, _proposal_, _commit_, _external_, _psk_), so p8 owns those names. Rename p5\u0027s four to TestTreeKemRejectsShortPath / _UndecryptablePathSecret / _MismatchedPathKey / _TrailingBlankNodes in tree_valsem_test.go, and p6\u0027s to TestPublicMessageRefusesApplicationContent. p5 Amendment A.4 already states \u0027the ValSem-named tests belong to the Validation plan\u0027 â€” apply it to the task bodies, which still contradict it."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p6-framing Tasks 12/13 (lines 3319, 3486, 3679) vs p7-group-lifecycle Tasks 5/6/9 (lines 1697/1964, 2104/2354, 3393/3514)",
        "problem":  "type Proposal, type ProposalOrRef and type Commit are each declared in both plans, in package mls, with different field sets and different codecs. p6 Coordination item 1 flags this and explicitly leaves it unresolved (\u0027it should be settled on paper first\u0027). Unresolved, the package does not compile and criteria 1 (families 8, 12, 13-15), 2, 3 and 4 all fail at once.",
        "fix":  "Settle it here: p6 owns proposal_wire.go and commit_wire.go containing the structs and their codecs only (it needs them for the messages family-12 gate); p7 Tasks 5, 6 and 9 delete their type declarations and codec bodies and keep only ProposalType, the v1 parse gate, ProposalList/CachedProposal, the cache, and the path-required rule. Update p7\u0027s \u0027Interfaces consumed\u0027 block to list Proposal/ProposalOrRef/Commit under the Framing plan."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p7-group-lifecycle lines 58-67 and throughout (82 occurrences of `tls:\"...\"` struct tags, MarshalTLS/UnmarshalTLS at lines 1372, 1543, 1704; zero occurrences of syntax.NewWriter/NewReader) vs p1-syntax-codec Task 13 (line 2279)",
        "problem":  "p1 builds an explicit sticky-error Writer/Reader codec: `type Marshaler interface{ MarshalMLS(w *Writer) }`, `Unmarshal(bs []byte, v Unmarshaler) error`, no reflection and no struct tags. p7 is written end to end against a reflection codec: `syntax.Marshal(value any)`, `syntax.Unmarshal(data []byte, value any) (int, error)`, `MarshalTLS() ([]byte, error)`, `UnmarshalTLS(data []byte) (int, error)` and `tls:\"head=varint\"` / `tls:\"optional\"` tags. Not one wire type in the largest plan will encode. p5 has an Amendment A.1 reconciling to p1\u0027s real API; p4 and p6 match p1; p7 and p8 do not.",
        "fix":  "Add an \u0027Amendment A\u0027 to p7 in the same shape as p5\u0027s: rewrite the consumed block to p1\u0027s actual surface, replace every `tls:` tag with an explicit MarshalTo(w *syntax.Writer) error / UnmarshalX(r *syntax.Reader) method, and add the marshalBytes/writeVec helpers p5 already defines (p7 must import them rather than redeclare them). Do this before p7 Task 2, since Task 2 is the first tagged struct."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p8-validation-interop Task 11 (connect/mls/codec_table_test.go, lines 2365-2391) and its Consumes block at lines 394-401",
        "problem":  "The codec table that every one of the 9 fuzz targets and the entire differential oracle protocol addresses calls ParseExtension, EncodeExtension, ParseKeyPackage, EncodeKeyPackage, EncodeMLSMessage, ParseProposal, EncodeProposal, ParseWelcome and EncodeWelcome. No plan produces any of them. p5 produces UnmarshalExtension(r *syntax.Reader)/MarshalExtensions([]Extension), p6 produces ParseMLSMessage/MarshalMLSMessage (not Encode-), p7 owns Welcome and KeyPackage but exports neither a Parse nor an Encode byte-slice entry point. Acceptance criterion 4 cannot compile.",
        "fix":  "Add a single task â€” p8 Task 11a, or a shared adapter file connect/mls/codec_entry.go owned by p8 â€” defining the five Parse/Encode byte-slice pairs as thin wrappers over the owning plans\u0027 real methods (e.g. `func ParseWelcome(b []byte) (*Welcome, error) { w := \u0026Welcome{}; return w, syntax.Unmarshal(b, w) }`). Rename EncodeMLSMessage to MarshalMLSMessage to match p6, and list the wrappers as an explicit ask on p5 (Extension, KeyPackage), p6 (MLSMessage, Proposal) and p7 (Welcome)."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p8-validation-interop Task 33, connect/.github/workflows/mls-interop.yml lines 6542, 6546, 6550 (`go run ./test-runner ...`); Task 6 vendoring script lines 1560-1572; Task 25 GENERATE.md lines 4978-4983",
        "problem":  "Gate 2 is executed by `go run ./test-runner` inside connect/mls/interop, but no task in any plan vendors the mlswg test runner. Task 6 copies only test-vectors/*.json; Task 25 copies only interop/proto/mls_client.proto. PINS.md claims the mlswg pin covers \u0027the 16 vector families and the gRPC test runner\u0027 but nothing brings the runner into the tree. `cmd/merge-runner-output` is named at line 6568 as \u0027written as part of this task\u0027 but has no Files entry, no test and no code. The interop harness â€” acceptance criterion 2 â€” has no runner to run.",
        "fix":  "Add p8 Task 25a: vendor mlswg `interop/test-runner/` (and its config JSONs: welcome_join.json, commit.json, application.json, branch.json, external_join.json, external_proposals.json, reinit.json, deep_random.json) into connect/mls/interop/test-runner/ and configs/ at the pinned commit, with a digest manifest like VECTORS.sha256, and confirm the runner actually accepts the `-json-out` flag the workflow passes (it does not upstream â€” add the flag in the vendored copy or parse its stdout instead). Promote cmd/merge-runner-output to its own numbered task with the dedup table test the plan describes."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p7-group-lifecycle lines 306-315 (\u0027From the Validation and interop harness plan ... profile.go\u0027) vs p8-validation-interop line 392 and Task 28 line 5318 (\u0027mls.DefaultProfile (Group lifecycle plan)\u0027); p2 line 126 and p4 line 76 both attribute profile.go to the Validation plan",
        "problem":  "profile.go is attributed circularly and created by nobody. p8\u0027s File Structure table has no profile.go row and no task creates Profile, DefaultProfile, CheckWireFormat, CheckProposalType or AllowPublicMessage. p7 calls DefaultProfile() at seven sites including NewGroup and the passive-client runner; p8\u0027s forge calls it at line 3633 and rpc_core at line 5528. Spec A Â§4.1 also requires profile_test.go and TestProfileIsClosed, which appear in no plan at all. Without it criteria 1 (families 13-15), 2 and 3 (ValSem240-246, 401-403) all fail.",
        "fix":  "Add a numbered task to p8 Phase A (it already owns errors.go, which Profile\u0027s refusals return): create connect/mls/profile.go with Profile, DefaultProfile(), the six Check* methods, the AllowPublicMessage field p7 Task 23 needs, plus profile_test.go with TestProfileIsClosed asserting the allowed-set tables equal Spec A Â§3.1/Â§3.2 exactly. Change p8\u0027s line 392 and Task 28\u0027s Consumes to list Profile as produced here, not consumed."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p8-validation-interop Task 7, connect/mls/vectors_test.go lines 1737-1779, and Task 13\u0027s `vectors` CI job lines 2686-2687",
        "problem":  "The vector-family registry is the mechanism p8 designed for Gate 2, but RegisterVectorFamily is called by exactly one file in the whole plan set (p8\u0027s own family 16). p1/p2/p3/p4/p5/p6/p7 all write standalone runners (TestTreeMathVectors, TestVectorKeySchedule, TestPassiveClientWelcomeVectors, ...) and none registers. expectedPendingFamilies therefore stays [1..15] forever and TestVectorManifestIsComplete passes green; TestVectorFamiliesVerify runs one family. The `vectors` CI job uses `-run \u0027TestVectorFilesArePinned|TestVectorManifestIsComplete|TestVectorFamiliesVerify|TestVectorGenerateThenVerify\u0027`, and no workflow anywhere runs `go test ./mls/` unfiltered (p1 covers ./mls/syntax/ only; p3\u0027s Definition of done explicitly asks the Validation plan to wire four gate commands that it never wires). Criterion 1 is green in CI with 15 of 16 families never executed.",
        "fix":  "Two changes. (a) Add a `RegisterVectorFamily` init() to each owning plan\u0027s KAT task â€” p3 T7, p2 T17, p4 T16/T17/T20/T25, p6 T16/T17, p5 T24/T25/T29, p7 T17/T23 â€” each removing its number from expectedPendingFamilies in the same commit, and state in p8 Task 7 that expectedPendingFamilies must be [] by the end of A4. (b) Add a `unit` job to mls-vectors.yml running `go test ./mls/... -count=1 -race` with no -run filter, and assert its presence in TestWorkflowsPinTheToolchain."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p8-validation-interop \u0027Open asks on other plans\u0027, lines 6609-6625, cross-checked against p2, p6 and p7",
        "problem":  "The forge (Task 17) and all 46 ValSem tests are built on eleven test seams, of which only Group.Close() and MakeProposalRef exist in a producing plan â€” and MakeProposalRef\u0027s signature differs (p2 line 3107 returns []byte, p8 asks for (ProposalRef, error)). Missing entirely: NewCryptoProviderWithRandom (p2 produces only NewCryptoProvider), Group.sealFramedContentForTest, Group.sealFramedContentWithPaddingForTest, FramedContentAuthData.MembershipTag (p6\u0027s struct is {Signature, ConfirmationTag} only), FramedContent.RawProposal, CommitOptions.SkipValidation, Commit.DropConfirmationTag, Commit.ConfirmationTagOverPreCommitTranscript, Group.OwnLeafNodeCopy, Proposal.UnknownType/UnknownBody, and ParseRatchetTree/EncodeRatchetTree/ValidateRatchetTree/OptionalNode. Acceptance criterion 3 has no way to construct a single malformed input.",
        "fix":  "Convert the ask table into concrete steps in the producing plans before execution: add NewCryptoProviderWithRandom to p2 Task 3; add the three test seams (sealFramedContentForTest, sealFramedContentWithPaddingForTest, MembershipTag field, RawProposal field) to p6 Tasks 3, 7 and 10; add SkipValidation, DropConfirmationTag, ConfirmationTagOverPreCommitTranscript, OwnLeafNodeCopy and the Proposal.Unknown* arms to p7 Tasks 9, 12 and 13. For the ratchet-tree trio, see the separate finding on ParseRatchetTree."
    },
    {
        "severity":  "MAJOR",
        "location":  "p1 Task 7 line 1174, p2 Tasks 9/17 lines 1972/3603, p3 Task 1 line 266, p5 Task 1 line 510 (all `connect/mls/testdata/vectors/PINS.md`); p6 line 4518 and p7 lines 341/8826 (`connect/mls/interop/PINS.md`); p8 Task 6 line 1476 (`connect/mls/PINS.md` + `testdata/vectors/VECTORS.sha256`)",
        "problem":  "Three different pin-file paths and three incompatible formats for one pinned mlswg commit. p1 Task 7 says in bold \u0027Do not create a second pin file\u0027; p8 creates the second and third. p6 greps `^mlswg_commit:` and p7 greps `mlswg=\\K\\S+` from interop/PINS.md; p8 writes a markdown table row with the commit in backticks â€” neither grep matches, so the vendoring commands in p6 T16/T17 and p7 T17/T23 expand to an empty commit and fetch a 404. Separately, p2 vendors hpke-rfc9180-x25519.json into the same testdata/vectors/ directory, and p8\u0027s `sha256sum *.json \u003e VECTORS.sha256` will therefore list 17 files, tripping `VECTORS.sha256 lists 17 files, spec A Â§4.2.1 names 16`. Spec A Â§4.2.4 names connect/mls/interop/PINS.md as the location.",
        "fix":  "Pick Spec A\u0027s path. Make connect/mls/interop/PINS.md the single pin file with one machine-readable line `mlswg=\u003csha\u003e` (plus the openmls sha and the three image digests), and have p1 Task 7 create it there. Change p2/p3/p5 to append to it rather than to testdata/vectors/PINS.md, delete p8\u0027s connect/mls/PINS.md, keep VECTORS.sha256 but move p2\u0027s hpke-rfc9180-x25519.json and xwing-draft10.json to testdata/vectors/rfc/ so the 16-file assertion stays exact."
    },
    {
        "severity":  "MAJOR",
        "location":  "p8-validation-interop Task 33, connect/mls/interop/docker-compose.yml lines 6497-6509 and mls-interop.yml lines 6534-6558",
        "problem":  "The Gate-2 workflow cannot run as written. (a) The runner executes on the host via `go run` but addresses peers by compose service name â€” `ours`, `openmls`, `mlspp`, `mls-rs` â€” which do not resolve on the host. (b) Ports contradict the compose file: openmls is published on 50053 but runs 1 and 2 address `${{ matrix.peer }}:50052`, so the openmls matrix row targets a closed port and run 3 becomes `-client openmls:50052 -client openmls:50053`; mls-rs is published on 50054 but addressed as :50052. (c) `run: /tmp/urmessage-mls-client -port 50051 ... \u0026` backgrounds a process in a step whose shell exits immediately, killing the client. (d) The \u0027assert zero leaked states\u0027 step reads mls/interop/out/leaked-states, which nothing writes â€” main.go\u0027s leak check sits after server.Serve, which never returns.",
        "fix":  "Address every client as localhost:\u003cpublished port\u003e (ours 50051, openmls 50053, mlspp 50052, mls-rs 50054) and drive the port from a matrix-keyed map rather than a literal 50052. Start the client with `nohup ... \u0026` plus a readiness wait (or as a compose service built from the branch). Have main.go install a SIGTERM handler that calls server.GracefulStop and then writes registry.liveCount() to the path named by a `-leak-out` flag. Update TestInteropWorkflowRunsBothRoles\u0027s literal string assertions to match."
    },
    {
        "severity":  "MAJOR",
        "location":  "Spec A Â§4.2.2 line 668 and Â§4.2.3 lines 656-657, versus p8-validation-interop Task 33 (matrix config list, line 6528) â€” referenced as existing by p6 line 1751 and p7 line 8849",
        "problem":  "Spec A requires a separate nightly interop matrix with `-public` to keep the PublicMessage path and ValSem007/008 honest, and a nightly `deep_random.json` run with a logged seed. Neither exists in any plan: mls-interop.yml is push/PR only, always passes `-private`, and its config list omits deep_random.json. Both p6 (Task 6, justifying that PublicMessage is not dead code) and p7 (Task 23, justifying AllowPublicMessage) cite the nightly -public matrix as though it were owned elsewhere. Acceptance criterion 2 is therefore only half delivered.",
        "fix":  "Add p8 Task 33a: a `schedule:`-triggered job in mls-interop.yml running the same peer matrix with `-public` over welcome_join/commit/application, plus a deep_random.json run with the seed echoed into the job log and the wiredump uploaded unconditionally. Add the corresponding rows to profile-reject.json and extend TestInteropWorkflowRunsBothRoles to assert `-public` and `deep_random` appear."
    },
    {
        "severity":  "MAJOR",
        "location":  "p3-tree-math produced surface (lines 2862-2890) versus p4 lines 262-271, p7 lines 91-101, p8 lines 282-290",
        "problem":  "Four incompatible spellings of the tree-math API. p3 ships `type LeafCount uint32`, `Root(n LeafCount) (NodeIndex, error)`, `DirectPath(x NodeIndex, n LeafCount) ([]NodeIndex, error)`, `Copath(x NodeIndex, n LeafCount) ([]NodeIndex, error)`, `CommonAncestor(x, y NodeIndex) NodeIndex`. p4 consumes `Root(leafCount LeafIndex) NodeIndex` and `NodeWidth(leafCount LeafIndex) NodeIndex` (wrong param type, wrong arity, wrong return type). p7 consumes `type TreeSize uint32`, `(self TreeSize) Root() NodeIndex` and `CommonAncestor(x, y LeafIndex)` â€” TreeSize does not exist and Root is not a method. p8 consumes `Root(leafCount uint32) NodeIndex` and `DirectPath(x LeafIndex, leafCount uint32)`. Only p5 carries a reconciliation (Amendment A.2). p4\u0027s Task 1 compile assertions will fail on first execution.",
        "fix":  "Treat p3\u0027s line 2862-2890 block as normative and rewrite the consumed blocks in p4 (line 262), p7 (line 91) and p8 (line 282) verbatim from it, including the error returns. Where p7 wants TreeSize semantics, alias it: `type TreeSize = LeafCount` in p7\u0027s own file, and replace `size.Root()` with `Root(size)` with the error handled."
    },
    {
        "severity":  "MAJOR",
        "location":  "p8-validation-interop Task 20 line 4345 and the ask table line 6623, versus p5-treekem line 398 (`UnmarshalRatchetTree(data []byte)`, `(*RatchetTree).Marshal()`) and p7-group-lifecycle line 160 (`ParseRatchetTree(crypto CryptoProvider, data []byte)`, `(*RatchetTree).Encode()`, `HasTrailingBlankNodes()`)",
        "problem":  "TestValSem300_TrailingBlankNodes calls ParseRatchetTree(b), EncodeRatchetTree(t), ValidateRatchetTree(crypto, b, groupId) and type OptionalNode, and attributes all four to the Tree math plan â€” which owns none of them (p3 is pure integer arithmetic, imports only errors and math/bits). p5 owns the tree and exports different names; p7 consumes a third signature. ValSem300 is one of the 43 and cannot be written.",
        "fix":  "Re-attribute the ask to p5 (it owns tree.go) and pick p5\u0027s names: rewrite p8 Task 20\u0027s ValSem300 test to use `UnmarshalRatchetTree`, `(*RatchetTree).Marshal`, `(*RatchetTree).Validate(ctx)` and p7\u0027s `HasTrailingBlankNodes()`. Add `type OptionalNode struct{ Present bool; Node Node }` plus an exported test seam for appending a blank to p5 Task 8, and fix p7\u0027s line 160 to drop the crypto parameter so there is one ParseRatchetTree signature."
    },
    {
        "severity":  "MAJOR",
        "location":  "p7-group-lifecycle Task 23, connect/mls/passive_client_vectors_test.go line 8763 versus the note at lines 8833-8844",
        "problem":  "The runner body sets `Profile: DefaultProfile()`, while the plan\u0027s own step-3 note says the passive-client vectors deliver handshake messages as PublicMessage and DefaultProfile refuses PublicMessage by group policy. It then defines `vectorProfile()` â€” which the test never calls. As written, families 13, 14 and 15 fail, and `ran == 0` is not the failure they hit; JoinFromWelcome or ProcessMessage errors first. Three of the 16 families of criterion 1 are red.",
        "fix":  "Change line 8763 to `Profile: vectorProfile()` and move the vectorProfile helper into step 1 so the failing test is honest about the seam it needs. Add AllowPublicMessage to the profile.go task (see the profile.go finding) as a field DefaultProfile leaves false."
    },
    {
        "severity":  "MAJOR",
        "location":  "p8-validation-interop Task 5, connect/mls/forbidden_test.go lines 1356-1359 and scripts/check-forbidden.sh line 1450",
        "problem":  "The Spec A Â§5.9 G8 gate scans roots {\"validation.go\", \"framing.go\", \"../message/writeauth.go\"}. No plan creates connect/mls/validation.go â€” p7 creates validate_proposals.go and validate_commit.go, p5 creates tree_sync.go. goFilesUnder returns nil when os.Stat fails, so the missing path is scanned silently and the gate passes vacuously; the shell mirror behaves the same way (grep exits 2 inside an `if`, which errexit does not catch). p6\u0027s framing files are framing.go, framing_preimage.go and framing_protect.go, of which only the first is listed.",
        "fix":  "Make a missing root a test failure rather than a no-op (`t.Fatalf(\"forbidden-crypto root %s does not exist â€” the gate is scanning nothing\", root)`), and replace the literal file list with a glob over connect/mls/valid*.go plus framing*.go plus tree_sync.go. Add `set -o pipefail` handling to check-forbidden.sh so a missing file is an error, and keep p6\u0027s TestFramingUsesConstantTimeComparison as the framing-side belt and braces."
    },
    {
        "severity":  "MAJOR",
        "location":  "p8-validation-interop Task 12 seedCorpus (lines 2557-2576) and mls-fuzz.yml (lines 2734-2761, 3415-3444); Spec A Â§4.4 line 882",
        "problem":  "Two gaps in criterion 4\u0027s plumbing. (a) Spec A requires the seed corpus to be the 16 vector families, the harvested interop wire dumps and OpenMLS\u0027s own corpora under testdata/corpus/; no task builds it. seedCorpus silently falls back to three trivial seeds when testdata/corpus/\u003cname\u003e/{bytes,structured} is absent, and the comment says \u0027the corpus job creates it\u0027 â€” there is no corpus job. p6 Task 18 step 3 tries to `go run ./interop/oracle/seedgen`, a tool no plan defines. (b) The fuzz matrix lists only p8\u0027s nine targets; the eleven other Fuzz functions in the same package (p2\u0027s four, p3\u0027s FuzzTreeMath, p4\u0027s two, p5\u0027s two) and p1\u0027s three in mls/syntax are in no per-commit workflow except p1\u0027s own.",
        "fix":  "Add a p8 task creating connect/mls/interop/cmd/seedgen (reads the 16 vendored vector files plus mls/interop/out/wiredump/*.bin and writes testdata/corpus/\u003cname\u003e/{bytes,structured}), commit the generated corpus, and make seedCorpus fail rather than fall back when the folder is missing. Extend mls-fuzz.yml\u0027s fuzz-short matrix with FuzzTreeMath, FuzzRatchetTreeDecode, FuzzUpdatePathDecode, FuzzGroupContextRoundTrip, FuzzPreSharedKeyIdRoundTrip, FuzzHpkeOpenBase, FuzzHpkeDeriveKeyPair, FuzzXwingDecapsulate and FuzzParseXwingPublicKey."
    },
    {
        "severity":  "MINOR",
        "location":  "p8-validation-interop Task 12, TestFuzzTargetsCoverEveryKind (lines 2468-2489)",
        "problem":  "The test that is supposed to catch \u0027a decoder added without a target\u0027 counts a hardcoded literal slice of nine CodecKind values written by hand in the test body. It has no connection to the declared Fuzz functions, so deleting FuzzWelcomeDecode leaves it green â€” the exact failure it exists to prevent.",
        "fix":  "Parse the file with go/ast (the plan already does this in errors_test.go) and collect FuncDecls whose name starts with \u0027Fuzz\u0027 and whose single param is *testing.F, then assert the set is exactly the nine names in Spec A Â§4.4\u0027s table and that every CodecKind is addressed at least once."
    },
    {
        "severity":  "MINOR",
        "location":  "p1-syntax-codec Task 7 (connect/mls/syntax/vectors_test.go, TestVectorDeserialization) and p8-validation-interop Task 8 (connect/mls/vectors_deserialization_kat_test.go, verifyDeserializationVector)",
        "problem":  "Family 16 is implemented twice, in two packages, against two different varint APIs. p1 uses the method form NewReader(b).ReadVarint() (uint32, error) that it actually ships; p8 calls package-level syntax.ReadVarint(b) (uint64, int, error) and syntax.WriteVarint(dst, v) []byte, which p1 does not produce, so p8\u0027s copy does not compile. p8 also re-vendors deserialization.json that p1 already vendored.",
        "fix":  "Keep p1\u0027s runner as the family-16 implementation. Reduce p8 Task 8 to a registry shim â€” `RegisterVectorFamily{Number: 16, Verify: func(t, raw){ syntax.VerifyDeserializationVector(t, raw) }}` calling an exported test helper in package syntax â€” or, simpler, register family 16 as covered-elsewhere and have TestVectorManifestIsComplete accept an ExternalTest field naming p1\u0027s test. Drop the duplicate vendoring step from p8 Task 6\u0027s loop."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p2-crypto-primitives.md:142-143 (producer) vs p4:227-228, p5 (69 uses), p6:165, p7:73, p8:267-268",
        "problem":  "The most-referenced constant in the slice is misspelled in every consuming plan. P2, which owns suite.go, produces `CipherSuiteX25519ChaCha20Sha256Ed25519` and `CipherSuiteX25519AesGcm128Sha256Ed25519`. P4/P5/P6/P7/P8 all write `CipherSuiteX25519ChaCha20SHA256Ed25519` (108 uses combined), and the AES constant has three mutually exclusive spellings: `CipherSuiteX25519AesGcm128Sha256Ed25519` (P2), `CipherSuiteX25519AES128SHA256Ed25519` (P4:227), `CipherSuiteX25519AES128GCMSHA256Ed25519` (P8:268). Not one line of downstream code compiles.",
        "fix":  "Pick P2\u0027s spelling (it matches CODESTYLE\u0027s no-all-caps / `Id` not `ID` rule) and do a literal find-and-replace across P4, P5, P6, P7, P8: `CipherSuiteX25519ChaCha20SHA256Ed25519` -\u003e `CipherSuiteX25519ChaCha20Sha256Ed25519`, and `CipherSuiteX25519AES128SHA256Ed25519` / `CipherSuiteX25519AES128GCMSHA256Ed25519` -\u003e `CipherSuiteX25519AesGcm128Sha256Ed25519`. Add both names to P2\u0027s Task 3 as the authoritative block other plans copy verbatim."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p1:2289-2304 (producer) vs p6 (200 uses), p5 (45 uses), p7 (82+38 uses), p4:99/161",
        "problem":  "Four incompatible codec conventions sit downstream of one producer. P1 ships `Marshaler{ MarshalMLS(w *Writer) }` (no error return) and `Unmarshaler{ UnmarshalMLS(r *Reader) error }`. P6 writes `MarshalMLS(w *syntax.Writer) error` â€” the extra return means P6\u0027s types do not satisfy `syntax.Marshaler`. P5 writes `MarshalTo(w *syntax.Writer) error` â€” a different method name entirely. P7 writes `MarshalTLS() ([]byte, error)` plus `tls:\"head=varint\"` struct tags and calls `syntax.Marshal(value any)`, i.e. a reflection codec P1 never builds. P4 writes a standalone `Marshal() ([]byte, error)`. Consequence beyond compilation: P1 Task 14 declares `CheckRoundTrip[T, PT interface{*T; Codec}]` is \"the shared property every downstream fuzz target calls\" and names `FuzzKeyPackageDecode`/`FuzzMlsMessageDecode` as callers â€” no downstream type satisfies `Codec`, so Gate 4 property 2 has no implementation path anywhere.",
        "fix":  "Freeze P1\u0027s interface as the single convention and state it once in every plan\u0027s Global Constraints: `MarshalMLS(w *syntax.Writer)` with errors going to the Writer\u0027s sticky error, `UnmarshalMLS(r *syntax.Reader) error`. Then: strip the `error` return from all 200 P6 signatures (a `return err` becomes `w.setErr(err); return`); rename P5\u0027s 45 `MarshalTo` to `MarshalMLS` and drop their error returns, extending P5\u0027s Amendment A Â§A.1 with this rewrite rule; and rewrite P7\u0027s 82 `MarshalTLS`/`tls:` tag sites and 38 `syntax.Marshal(any)` calls as explicit Writer/Reader bodies. Add a compile assertion `var _ syntax.Codec = (*KeyPackage)(nil)` per wire type so drift fails at build."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p7-group-lifecycle.md:56-67 and throughout (no reconciliation amendment)",
        "problem":  "P7\u0027s entire consumed-syntax contract describes a codec that does not exist: `Marshal(value any)`, `Unmarshal(data []byte, value any) (int, error)`, `MarshalTLS()/UnmarshalTLS()` interfaces, and `tls:\"head=varint\"` / `tls:\"optional\"` / `tls:\"omit\"` struct tags. P1 ships a hand-written Writer/Reader with none of that. P7 covers this with one sentence at line 52-54 (\"that plan wins and the call sites in this plan are adjusted\") â€” an instruction to rewrite ~8,900 lines of concrete code with no rule for how. P5 hit the same drift and handled it properly with Amendment A (exact name table, `marshalBytes`/`writeVec` adapters, a named list of every affected function); P7 has no equivalent. P6\u0027s own coordination item #3 (p6:5121-5124) predicted exactly this: \"If wave 1 shipped reflection, every MarshalMLS body here changes\" â€” wave 1 shipped the opposite, so P7 is the plan that must change.",
        "fix":  "Add an Amendment A to P7 modelled on P5\u0027s Â§A.1: a name table (`syntax.Marshal(any)` -\u003e `syntax.Marshal(Marshaler)`, `Unmarshal(...)(int,error)` -\u003e `Unmarshal(...)error`, `ErrVectorTooLong` -\u003e `ErrLengthExceedsMax`, `MaxVectorLength uint64` -\u003e `int`), a worked rewrite of one tagged struct into an explicit `MarshalMLS`/`UnmarshalMLS` pair, and the full list of the 82 tagged types requiring the transform. Delete every `tls:` tag from P7\u0027s task bodies before execution."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p6:93-94, 344-399, Tasks 12-13 vs p7:327, 331, 1697, 2104, 3393, Tasks 5-6-9",
        "problem":  "Two plans implement the same types in the same Go package. P6 creates `proposal_types.go` and `commit_types.go` declaring `ProposalType`, `Proposal`, `ProposalRef`, `ProposalOrRef` and `Commit`; P7 creates `proposal.go`, `proposal_list.go` and `commit.go` declaring the identical five types. Same for `const PastEpochWindow uint64 = 32`, declared by P4 (key_schedule.go, p4:117/1206) and by P7 (p7:383/540). All are `package mls`, so this is a duplicate-declaration compile failure that stops both plans dead. P6\u0027s own coordination item #1 (p6:5114-5118) flags it as unsettled and asks for it to be \"settled on paper first\" â€” it was not settled, and P7\u0027s file structure still claims the types.",
        "fix":  "Settle it in the plans, not at compile time. Give the wire types to P6 (its `messages` vector family cannot be green without them) and strike them from P7: P7 Task 5 keeps only the v1 parse gate and `ParseProposal` refusals, Task 6 keeps only the proposal cache and resolution, Task 9 keeps only the path-required rule and `StagedCommit` â€” all operating on P6\u0027s types. Delete `type ProposalType/Proposal/ProposalRef/ProposalOrRef/Commit` from p7:1697, 1896, 1964, 2104, 2354, 3393, 3514 and from its file-structure rows 327-331. Assign `PastEpochWindow` to P4 alone and delete p7:383/540, leaving P7 to reference it."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p8-validation-interop.md:6605-6626 (\"Open asks on other plans\"), plus 394-401 and Task 11:2268-2271",
        "problem":  "P8 owns three of the four acceptance gates (Gate 2 interop, Gate 3 the 43 ValSem codes, Gate 4 fuzzing) and its code depends on ~23 symbols that no plan produces. The plan states this itself: \"Each is referenced by real code above and will not compile without it.\" Verified absent from every other plan: `NewCryptoProviderWithRandom`, `sealFramedContentForTest`, `sealFramedContentWithPaddingForTest`, `FramedContentAuthData.MembershipTag`, `FramedContent.RawProposal`, `CommitOptions.SkipValidation`, `Commit.DropConfirmationTag`, `Commit.ConfirmationTagOverPreCommitTranscript`, `Group.OwnLeafNodeCopy`, `Proposal.UnknownType/UnknownBody`, `BasicCredential()`, `ValidateRatchetTree`, `EncodeRatchetTree`, `OptionalNode`, and 8 of the 10 `Parse*`/`Encode*` pairs Task 11\u0027s codec table consumes (`ParseExtension`, `EncodeExtension`, `ParseKeyPackage`, `EncodeKeyPackage`, `EncodeMLSMessage`, `EncodeProposal`, `ParseWelcome`, `EncodeWelcome`). An \"ask\" recorded in a table at the end of a plan is not an owned task; nobody will build these.",
        "fix":  "Convert every row of the open-asks table into a numbered task in the producing plan, with its own failing test and commit, and reference that task number from P8. Concretely: add a task to P2 for `NewCryptoProviderWithRandom(suite, io.Reader)`; add a task to P6 for the four test seams (`sealFramedContentForTest`, `sealFramedContentWithPaddingForTest`, `MembershipTag`, `RawProposal`) plus `EncodeMLSMessage`/`EncodeProposal`; add a task to P7 for the commit/group seams, `BasicCredential`, `MakeProposalRef`, and the `Parse*`/`Encode*` byte-slice wrappers; add a task to P5 for `ParseRatchetTree`/`EncodeRatchetTree`/`ValidateRatchetTree`/`OptionalNode`. P8\u0027s Tasks 17-23 must not be schedulable until those land."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p4-key-schedule.md:255-256, 265-270, 281-282, 398-411, 445-455 and Task 21:4214/4218/4244",
        "problem":  "Every wave-1 signature P4 pins is wrong, and P4\u0027s Task 1 tells the executor the result is PASS. Against P1: `WriteBytes` does not exist (it is `WriteRaw`), and `WriteOpaque` returns nothing, not `error`. Against P3: `NodeWidth` is `(LeafCount) uint32` not `(LeafIndex) NodeIndex`; `Root` is `(LeafCount) (NodeIndex, error)` not `(LeafIndex) NodeIndex`; `Level` is a method `x.Level()` not a free function. Against P2: `HpkePublicKey` is `[]byte` with no `Bytes()` method, yet p4:406 pins `_ func(HpkePublicKey) []byte = HpkePublicKey.Bytes`. Lines 255-256 are literal placeholders: `type HpkePrivateKey ...` / `type HpkePublicKey ...`. The drift is not confined to the pin file â€” Task 21 writes `root := Root(leafCount)` (single-value, p4:4214), `width: NodeWidth(leafCount)` (p4:4218) and `for Level(current) \u003e 0` (p4:4244). Task 1 Step 4 says \"Expected: PASS\", so an executor with zero context hits a wall of compile errors the plan says should not happen, with no guidance on which side is authoritative.",
        "fix":  "Rewrite p4:223-300 against the landed signatures and fix the call sites: replace lines 255-256 with `type HpkePrivateKey []byte` / `type HpkePublicKey []byte` and delete the `HpkePublicKey.Bytes` pin (compare `external_pub` against the slice directly). Replace the tree-math pins with `NodeWidth(LeafCount) uint32`, `Root(LeafCount) (NodeIndex, error)`, `(NodeIndex).Level() uint32`, and update Task 21 to `root, err := Root(LeafCount(leafCount))` with an error return, `width: NodeWidth(LeafCount(leafCount))`, and `for current.Level() \u003e 0`. Replace `w.WriteBytes` with `w.WriteRaw` and drop the `error` from the `WriteOpaque` pin at p4:452/457. Then change Task 1 Step 2\u0027s expected failure to the missing vector file only."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p7:307-313, 2636, 8841-8846; p8:392 and File Structure (lines 67-116); p2:126; p4:76",
        "problem":  "`connect/mls/profile.go` has circular ownership and is therefore created by nobody. P2:126, P4:76 and P7:2636 all state the Validation plan owns it. P8\u0027s File Structure never lists it, and P8:392 lists `type Profile struct { /* profile.go */ }` under \"consumed From Group lifecycle\". So P7 points at P8 and P8 points at P7. P7 Task 11\u0027s `NewGroup` calls `DefaultProfile()` and `profile.CheckCiphersuiteForCreate(cfg.Suite)` (p7:4452), P7:6393 calls it again in `JoinFromWelcome`, and p7:8841 sets `profile.AllowPublicMessage = true` â€” a field declared in neither plan. Group creation, the entry point of the whole slice, cannot be written.",
        "fix":  "Assign `connect/mls/profile.go` to P8 explicitly: add it to P8\u0027s File Structure table and add a Phase A task producing `type Profile struct{ AllowPublicMessage bool; ... }`, `DefaultProfile() *Profile`, `(*Profile).CheckCiphersuiteForCreate(CipherSuite) error` and the rest of the v1 narrow-profile gates, with its own failing test. Change P8:392 from a consumed symbol to a produced one, and change P7:307-313 to reference that task."
    },
    {
        "severity":  "BLOCKER",
        "location":  "p6:174-184 and Tasks 11, 16 vs p4:190-196",
        "problem":  "P6\u0027s `MessageKeySource` contract and P4\u0027s `SecretTree` share no method. P6 requires `NextMessageKey(contentType ContentType, leaf LeafIndex) (key, nonce []byte, generation uint32, err error)`, `MessageKey(contentType ContentType, leaf LeafIndex, generation uint32)` and `EraseMessageKey(contentType, leaf, generation)`. P4 produces `NextSenderKey(leaf LeafIndex, kind RatchetType) (generation uint32, key, nonce []byte, err error)`, `ReceiverKey(leaf LeafIndex, kind RatchetType, generation uint32)` and `SenderGeneration(...)`. Different names, different argument order, a `ContentType`/`RatchetType` type mismatch, and no erase method at all â€” yet P6\u0027s reuse guard (Task 11, ValSem006) is built on erase. P6\u0027s own coordination item #4 (p6:5125-5126) says \"Task 16 cannot run without them\", and Task 16 is the `message-protection` vector family, one of the 16 Gate 1 families.",
        "fix":  "Make P4 the producer of the interface P6 declares. Add a task to P4 defining `EraseMessageKey` and thin adapters on `*SecretTree` â€” `NextMessageKey`/`MessageKey`/`EraseMessageKey` mapping `ContentTypeApplication` to `RatchetApplication` and `ContentTypeProposal`/`ContentTypeCommit` to `RatchetHandshake`, with `NextMessageKey` reordering the return to `(key, nonce, generation, err)`. Add `var _ MessageKeySource = (*SecretTree)(nil)` to P6 Task 11 so the mismatch fails at build rather than at Task 16."
    },
    {
        "severity":  "MAJOR",
        "location":  "p8-validation-interop.md Task 23, Step 3 (\"Write minimal implementation\")",
        "problem":  "The implementation step for errata 8745 and 8815 â€” named explicitly in acceptance criterion 3 â€” contains no code. It is three prose sentences (\"`validation.go` gains `checkErrata8745(path *UpdatePath, context *GroupContext) error`, called from the commit path\") and then disclaims ownership: \"Three changes, in files the Group lifecycle plan owns; this task is what forces them and the diff belongs to whichever plan lands second.\" The negative tests are specified; the production code that makes them pass belongs to nobody, in a plan that says it does not own the file. Step 5 nonetheless stages `mls/validation.go` and `mls/group.go` in the commit.",
        "fix":  "Write the two functions out in full in Step 3 â€” `checkErrata8745` walking the update-path leaf\u0027s `Capabilities.ExtensionTypes` against every GroupContext extension type, and `checkErrata8815` checking each `ProposalRef` against the pending map before any other commit processing â€” and name their call sites by line in P7\u0027s Task 18 (commit processing). Add the `DeleteGroupStateBefore(self.groupId, self.epoch - PastEpochWindow)` call, guarded by `self.epoch \u003e PastEpochWindow`, to P7 Task 19 as an explicit step rather than leaving it to \"whichever plan lands second\"."
    },
    {
        "severity":  "MAJOR",
        "location":  "p7-group-lifecycle.md Task 11 (724 lines), also Tasks 13 (567), 18 (564), 7 (545), 3 (529), 6 (509)",
        "problem":  "P7 Task 11 is not one TDD cycle. In a single failing-test-to-commit loop it introduces `StateStore` (8 methods), `GroupConfig`, `Member`, `EpochSecretName`, the `Group` type, `NewGroup`, and twelve `Group` methods (`GroupId`, `Epoch`, `OwnLeafIndex`, `Members`, `MemberAt`, `EpochAuthenticator`, `Export`, `EpochSecret`, `RatchetTree`, `GroupContext`, `GroupPolicy`, `Close`) plus a full in-memory test store. A failure anywhere in that surface gives one red bar over ~700 lines with no bisection point, and the commit is unreviewable. Five more P7 tasks exceed 500 lines with the same shape.",
        "fix":  "Split Task 11 into four: (11a) `StateStore`, `GroupConfig`, `Member`, `EpochSecretName` and the `testStore` fixture, tested by a store round-trip; (11b) the `Group` struct and `NewGroup` following the RFC Â§11 four steps, tested by epoch 0 / one-member tree / empty confirmed transcript hash; (11c) the read-only accessors; (11d) `Export`, `EpochSecret` and `Close` with the zeroization assertion. Apply the same split to Tasks 3, 6, 7, 13 and 18 â€” target roughly 150-200 lines per task, which is what the rest of the plan set averages."
    },
    {
        "severity":  "MAJOR",
        "location":  "p8-validation-interop.md:249-261 and 282-290",
        "problem":  "P8\u0027s consumed contracts for syntax and tree math are wrong in every element, so its Phase A tasks (which the plan says depend on nothing but `connect/mls/syntax` and are schedulable in wave 1) cannot build. Against P1: `Marshal(v any)` / `Unmarshal(b []byte, v any) error` (P1 takes `Marshaler`/`Unmarshaler`); `ReadVarint(b []byte) (uint64, int, error)` and `WriteVarint(dst []byte, value uint64) []byte` as free functions (P1 has `(*Reader).ReadVarint() (uint32, error)` and `(*Writer).WriteVarint(uint32)` as methods over `uint32`); `ErrNonMinimalLength` (P1: `ErrVarintNotMinimal`); `ErrVectorTooLong` (P1: `ErrLengthExceedsMax`). Against P3: `Root(leafCount uint32) NodeIndex`, `DirectPath(x LeafIndex, leafCount uint32) []NodeIndex`, `Copath(x LeafIndex, leafCount uint32) []NodeIndex` â€” P3 ships `Root(LeafCount) (NodeIndex, error)`, `DirectPath(NodeIndex, LeafCount) ([]NodeIndex, error)`, `Copath(NodeIndex, LeafCount) ([]NodeIndex, error)`.",
        "fix":  "Replace p8:249-261 and 282-290 with the blocks copied verbatim from p1:387-397/623-634/2300-2303 and p3:2860-2890, then fix P8\u0027s call sites: varint access becomes `syntax.NewReader(b).ReadVarint()`, and every `Root`/`DirectPath`/`Copath` call gains a `LeafCount(...)` conversion and an error return. Add a `validation_deps_test.go` compile-pin task in Phase A, matching the one P4 Task 1 attempts, so the drift fails on day one."
    },
    {
        "severity":  "MAJOR",
        "location":  "p8-validation-interop.md Task 11 (lines 2261-2419)",
        "problem":  "Task 11 declares `CodecKind`, `CodecPair`, `CodecFor` and `CodecKinds` under \"Produces\" and states they are what \"the fuzz targets and the differential oracle both address\", but Step 3 writes them into `connect/mls/codec_table_test.go` â€” a `_test.go` file. Symbols in a test file are invisible outside the test binary of `package mls`. The Go oracle client (Task 15) and the interop module (Task 25, deliberately a separate module under `connect/mls/interop/`) cannot reference them, so the shared kind-id contract that is supposed to stop Go and Rust drifting about which decoder a divergence concerns does not actually exist across that boundary.",
        "fix":  "Move `CodecKind`, the five kind constants, `CodecPair`, `CodecFor` and `CodecKinds` into a non-test file â€” `connect/mls/codec_table.go` â€” leaving only `TestCodecTableIsClosed` and `TestCodecTableRejectsEmptyInput` in the `_test.go`. Update Task 11\u0027s Files block and its Step 5 `git add`, and have Task 15\u0027s oracle client import the constants rather than redeclaring the wire bytes."
    },
    {
        "severity":  "MAJOR",
        "location":  "p7:168, 178-183, 4036 vs p5:238-505, p4:104-135",
        "problem":  "Two more symbols P7\u0027s task bodies call are named by no producing plan. `HPKECiphertext` (P7, 7 uses) does not exist â€” P5 produces `HpkeCiphertext` (20 uses, and P8 agrees at p8:316); the all-caps form also violates the CODESTYLE rule the plans cite. `DeriveEpochSecretsFromEpochSecret`, consumed by P7 Task 11 (p7:4036) for group creation, appears in no other plan â€” P4 exposes the epoch secrets through `NewKeySchedule(...)` plus `(*KeySchedule).Secrets() *EpochSecrets`.",
        "fix":  "Replace all 7 `HPKECiphertext` occurrences in P7 with `HpkeCiphertext`. Replace the `DeriveEpochSecretsFromEpochSecret` call in P7 Task 11 with the real P4 sequence: build the `GroupContext`, call `NewKeySchedule(crypto, ZeroSecret(crypto), commitSecret, pskSecret, groupContext)`, and read `schedule.Secrets()`. Correct the Consumes line at p7:4036 to name `NewKeySchedule` and `KeySchedule.Secrets` instead."
    },
    {
        "severity":  "MINOR",
        "location":  "p4-key-schedule.md Global Constraints (lines 23-40) and Task 1 Step 5 (lines 537-544)",
        "problem":  "P4 is the only plan with no cwd statement in its Global Constraints, and its commands mix two conventions. P1/P3/P5 run from the `connect` module root (`./mls/...`); P6:64 and P8:61 explicitly run from the workspace root (`./connect/mls/...`). P4 uses workspace-root paths (`go test ./connect/mls/...`, `mkdir -p connect/mls/testdata/vectors`) but then runs bare `git ls-files | wc -l`, `git add connect/mls/...` and `git commit` from that directory. The workspace root `C:\\Users\\ryanm\\Downloads\\claude_sandbox_message` has no `.git` (verified â€” only `connect/.git` exists), so all three git commands fail with \"not a git repository\". P8 gets this right by using `git -C connect`.",
        "fix":  "Add the cwd line to P4\u0027s Global Constraints matching P6:64 (\"all commands run from the workspace root `C:\\Users\\ryanm\\Downloads\\claude_sandbox_message`\"), and rewrite every git command in P4 to the `git -C connect ...` form P8 uses, including the `git -C connect ls-files | wc -l` index-hazard check before and after each `git -C connect add`."
    },
    {
        "severity":  "MINOR",
        "location":  "p7-group-lifecycle.md Task 11, `stateKey` helper (lines 4124-4126)",
        "problem":  "The shared test store keys group state by `string(groupId) + \"/\" + string(rune(epoch))`. `epoch` is `uint64`; any value in the UTF-16 surrogate range 0xD800-0xDFFF or above 0x10FFFF converts to the same replacement rune U+FFFD, so distinct epochs collide into one map entry. This fixture is reused by Task 19 (the epoch advance and past-epoch window) and by P8\u0027s `TestValSem400_PastEpochBound`, where a silent key collision would look like a correct deletion.",
        "fix":  "Change `stateKey` to `groupId` plus a `strconv.FormatUint(epoch, 10)` suffix. While there, replace the O(epoch) inner loop in `DeleteGroupStateBefore` (lines 4141-4147) with a scan over the existing map keys, so the helper stays linear in stored epochs rather than in the epoch number."
    }
]
